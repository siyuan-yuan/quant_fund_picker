# V4.0 回测失效诊断与修复方案

> 研究结论基于仓库内现有真实净值/指数缓存、Point-in-Time 评分缓存及既有实验报告；不把截图读数当作精确统计量。附件 CSV 在本次运行环境中未挂载，因此截图中的交易笔数、逐笔成本和分年收益不能被重新计算，不能冒充已经完成了附件级复核。

## 1. 先给结论

这不是“V4 因子必然无效”的单一结论，而是至少有四个问题叠加：

1. **模型版本与执行层混在一起**：图标题是 `v38`，代码的 `STRAT_VERSION` 也是 V3.8；V4.0 只是评分引擎权重。比较 V3.7/V4.0 时，必须固定同一执行层，否则无法归因。
2. **V4 的原始优势被严重高估**：仓库的严格全历史 Model Zoo 已给出反证：V3.7 OOS IC `0.1127`，V4/Huber 为 `0.0876`；V3.7 的 full/2015+/2019+ Calmar 为 `0.33/0.27/0.64`，V4×0.5 为 `0.27/0.19/0.39`。因此“实验中 IC 上升 47%”不能作为生产升级依据，它来自较短、小池、宽松/含前视协议。
3. **V4 新增的 `earn_momentum` 证据窗口短且有明显状态依赖**：它只有约 2021-08 之后的 PE 历史；既有报告显示震荡/熊市有效，但牛市反向。固定给 20% 权重会在市场状态变化时拖累排序。
4. **回测执行存在需要先修复的可交易性问题**：评分在月末生成、候选在同一日用净值交易；这可能使用决策日后才可知的净值。即使它未必解释“V4 比 V3.7 差”，也会污染所有模型比较，必须先修正。

基于当前证据，**生产默认应回退 V3.7**；V4 只能作为经过严格 OOS 验证的 0%/20%/30% 试验分支，而不是默认模型。

## 2. 对截图的可验证观察

截图显示：策略从 2021-08 附近约 1.0 开始，2022 年后长期在约 0.80～0.87 区间，2025 年后才明显修复，2026 年中一度约 1.39，末端回落至约 1.10；沪深 300 末端约 0.95。由此可以确认：

- 相对基准的最终超额为正，但**收益路径不稳定，主要收益集中在 2026 年一段行情**；
- 2022～2025 长时间低于初始净值，说明模型/执行层在中期震荡和风格轮动中承受了较高机会成本；
- 2026 年尖峰后快速回撤，说明组合可能在少数高动量/高成长暴露上集中，固定 10 槽和分数阈值并没有充分限制风格拥挤；
- 图本身不能证明 V4 比旧版差，必须使用同起止日、同基金宇宙、同费用、同决策延迟的两组逐日净值做配对检验。

## 3. 已有大样本实验真正说明了什么

仓库 `output/model_zoo_report.md` 的严格实验是目前最有价值的证据：

| 项目 | 严格结果 |
|---|---:|
| 样本 | 235 个季度末、217 只基金、31,329 行、2006-09 至 2026-03 |
| V3.7 OOS IC | 0.1127，t=8.11 |
| V4/Huber OOS IC | 0.0876，t=4.89 |
| V3.7 full Calmar | 0.33 |
| V4×0.5 full Calmar | 0.27 |
| V3.7 2019+ Calmar | 0.64 |
| V4×0.5 2019+ Calmar | 0.39 |
| V3.7 交易笔数/胜率/平均持有 | 130 / 59% / 328 天 |
| V4×0.5 交易笔数/胜率/平均持有 | 181 / 50% / 174 天 |

这组结果支持的解释是：V4/ML 或额外前视腿改变了排序边界，造成更多换手、更短持有期和更多 CPPI/止损干预；成本、阈值迟滞和执行层共同把较弱的信号转化为更差的端到端结果。

注意：仓库另两份报告仍保留“V4.0 WF_IC=0.351、V4.0 最优”等旧结论，与严格 Model Zoo 结论冲突。它们不能同时作为证据，必须以**严格协议、较长样本、真正 OOS、同执行层**的结果为准。

## 4. 必须先做的代码/数据修复

### P0：统一版本和评分来源

当前代码有明显口径风险：

- `config.py` 的注释称默认 V4.0；
- `engine.finalize()` 直接将结果写入 `S_total`，并标记 `model_version = "V4.0"`；
- `backtest_local.py` 的 `score_from_raw()` 在存在 `S_engine` 时直接使用它，但在无 `S_engine` 时走 V3.7 公式，却仍把 `model_version` 标成 V4.0；
- 回测输出 tag 使用 `v38`，`STRAT_VERSION` 也明确是 V3.8。

修复：

1. 将评分版本作为显式参数：`--score-model v37|v40|enet30`；
2. `score_from_raw()` 返回实际模型版本，不允许 fallback 后伪造版本；
3. 输出中同时保存 `score_model`、`execution_model`、`score_cache_suffix`、`universe_hash`；
4. 每次比较必须校验四者相同，只允许 `score_model` 不同。

### P0：消除同日决策/交易

现有模拟在月末评分日期当天直接买卖，基金净值通常不能在该日收盘前知道。建议：

- 月末 `t` 只生成信号；
- `t+1` 的第一个可交易日执行买卖；
- 止损也采用可实现的下一估值日/下一交易日价格；
- 所有模型都使用同一延迟；
- 新增 `execution_lag_days={0,1,2}` 敏感性表，生产采用 1。

此外，报告中的期末总收益应在**期末清算并扣除卖出费**后计算。当前 `simulate()` 在曲线记录后才清仓，`report()` 使用清算前的 `ec.equity.iloc[-1]`，会漏计最后一笔卖出费用。

### P1：纠正 `earn_momentum` 的缺失处理

当前 `finalize()` 在截面有效 `earn_mom_raw` 少于 30 个时把整列设为 NaN；有效基金不足时，分数又按剩余因子重新归一化。这会造成不同日期实际使用不同的模型公式。

建议固定规则：

- 每个日期报告 `earn_valid_n` 和覆盖率；
- 覆盖率低于 70% 时 `w_earn=0`，其余权重按明确的 V3.7 权重归一化；
- 覆盖率达到 70% 后才允许 `w_earn>0`；
- 禁止“每只基金缺一个因子就自动改变分母”作为默认行为，改为统一缺失策略，并记录缺失率。

### P1：避免把行业 PE 的“盈利动量”称为前视信息

`earnings = close / PE` 是隐含盈利收益率，`pct_change(63).diff(63)` 是二阶变化。它不是分析师盈利预期，也不是已确认财报；应称为“PE 派生的行业盈利收益率加速度”。必须做：

- PE 发布日期/可获得日期滞后测试：0、1、5、20 个交易日；
- 使用修订前数据或至少禁止把后来修订值回填到历史；
- 在无 PE 的行业上验证是否改变基金覆盖；
- 只在 2021-08 以后单独报告该因子 OOS，不能把 2006 年起结果与它混合。

## 5. 严格实验矩阵

### 5.1 主比较

固定：同一 217/476 只基金集合、同一 PiT Top100、同一月末信号、同一执行层、同一费用、同一延迟 1 日。比较：

- V3.7；
- V4.0 固定权重；
- V4.0 去掉 earn；
- V4.0 `w_earn` = 0.10/0.20/0.30；
- V3.7 + Lasso/ElasticNet rank 预测腿 = 0.20/0.30；
- 仅改变执行层的基准组。

### 5.2 必报指标

每一组输出：CAGR、总收益、最大回撤、Calmar、Sortino、月度胜率、相对沪深300超额、换手率、交易笔数、胜率、盈亏比、平均/中位持有期、费用占比、现金占比、CPPI触发次数、止损次数、分年收益。

统计上必须报告：

- 每个月/季度的 Rank IC 均值、标准差、t 值；
- Q5-Q1 未来 6M/12M 收益差及 bootstrap 95% CI；
- 年度 block bootstrap（按年份或季度重采样），不能只看一条累计曲线；
- Diebold-Mariano 或配对 block bootstrap 检验 V4 与 V3.7 的收益差；
- 交易成本敏感性：买入/卖出各 0、5、15、30 bps；
- 延迟敏感性：0、1、2 个交易日；
- 费用与现金收益假设敏感性：现金 0%、1.5%、2.5%。

### 5.3 接受标准

不再以单一全样本 IC 或单一最终净值升级模型。V4 只有同时满足以下条件才能进入候选生产：

1. 至少三个独立时间窗口的 OOS Calmar 不低于 V3.7；
2. OOS IC、Q5-Q1 与端到端收益方向一致；
3. 交易成本加倍后仍不显著劣化；
4. block bootstrap 下相对 V3.7 的 95% 区间不主要落在负值；
5. 收益不能由单一年份或少数 10 笔交易贡献；
6. 延迟从 0 改为 1 日后结论不翻转。

## 6. 推荐落地版本

### 立即恢复

- 默认评分：V3.7；
- 保留 V4 作为离线研究分支；
- 执行层先不继续增加止损、CPPI 和 regime 规则，先固定并完成公平比较；
- 先修复版本标签、同日交易、期末清算费用和因子缺失口径。

### 若仍希望利用 V4 信息

优先试验而非直接上线：

```text
S_candidate = 0.70 * S_V3.7 + 0.30 * S_Lasso_F2_fwd6_rank
```

或在 V4 内部只给 `earn_momentum` 10%，并要求覆盖率达到阈值；不建议未经严格 OOS 证据使用固定 20%。仓库既有严格实验已经显示 Lasso/ElasticNet × 0.3 比 Huber V4×0.5 更稳健，但它仍不是在所有窗口都胜过 V3.7。

### 风控层建议

- 单基 20% trailing stop、CPPI -15/-20/-25 不应与评分模型同时调参；先锁定执行层；
- 对 2026 尖峰集中收益做 leave-one-year-out 和 top-10-trade exclusion；
- 增加风格暴露、前十大 RBSA 暴露、组合相关性和拥挤度报告；
- 若 2026 的大部分超额来自单一成长/海外风格，应设置风格簇上限，而不是再调高分数权重。

## 7. 最终判断

按现有严格真实数据证据，V4.0 不是“回测偶然变差”，而是其初始胜出证据被不严格协议放大；在更长的 PiT/OOS 检验中，V3.7 同时取得更高 IC、更高 Calmar、更高胜率和更低换手。当前最合理的修改不是继续拍脑袋改权重，而是：

1. 先让版本、数据时点、执行时点和成本口径可审计；
2. 默认回退 V3.7；
3. 按上述矩阵重新跑 V4/混合模型；
4. 只有在延迟、成本、跨时代和 bootstrap 检验均通过后，才恢复 V4 的部分权重。

仅供量化研究，不构成投资建议。
