# ============================================================================
# quant_fund_picker .gitignore
# 最后更新：2026-09-03
#
# 治理铁律（V5 重规范 §0）：
#   · 后端过程产物与 output/ 永远留在仓库外，只把产物指纹/路径写进 manifest；
#   · 结论进文档（台账/主契约），产物只是可重跑的中间实物；
#   · 禁止 git add -f 强推产物。
# ============================================================================

# ---------------------------------------------------------------------------
# Python 运行时 / 缓存
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so

# 虚拟环境
# 【2026-09-03 修复】原先只写 `.venv/`，匹配不上自定义名 `.venv_review/`，
# 导致 9,508 个 venv 文件（含 Windows .exe）被误提交进 main。
# 下列写法覆盖一切常见 venv 目录名。
.venv/
.venv*/
venv/
venv*/
env/
ENV/
.env
.python-version

# 测试 / 类型检查 / lint 缓存
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyright/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
coverage.xml

# 打包产物
build/
dist/
*.egg-info/
.eggs/

# ---------------------------------------------------------------------------
# 编辑器 / 操作系统
# ---------------------------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# ---------------------------------------------------------------------------
# 日志
# ---------------------------------------------------------------------------
*.log
logs/

# ---------------------------------------------------------------------------
# 数据源缓存（可按需重新抓取）
# 注：仓库内已有约 15,471 个历史基线文件是既成事实，不再提交新增缓存。
# ---------------------------------------------------------------------------
cache/

# ---------------------------------------------------------------------------
# 研究产物（回测、面板、扫描、实验输出）
# 默认全部不入库；仅白名单放行一份可启动快照与模型裁决报告。
#
# ⚠️ 这条规则会挡住 output/v5/*.csv、output/p1_panel/**、output/s61_panel/** 等
#    全部实验产物 —— 这是**设计如此**，不是 bug。
#    git add 时 git 会静默跳过它们，不报错也不提示，容易误以为已上传。
#    产物丢失可重跑；结论必须落在 docs/ 台账里。
# ---------------------------------------------------------------------------
output/*
!output/scan_20260810.csv
!output/model_zoo_report.md

# 本地私人数据：明确列出，提醒维护者绝不能强制提交。
output/ledger.json
output/dca_state.json
output/fee_settings.json
data/

# ---------------------------------------------------------------------------
# 临时 / 交接用文件
# ---------------------------------------------------------------------------
*.patch
*.diff
*.tmp
*.bak
*.orig
*.rej