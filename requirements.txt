flask>=3.0
waitress>=3.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
requests>=2.31
akshare>=1.14
# Windows 若仍出现 partition_address_space.cc / IsConfigurablePoolInitialized：
#   pip uninstall mini-racer py-mini-racer -y
#   pip install py-mini-racer==0.6.0
# 新版 mini-racer 内嵌 V8 12，多次 MiniRacer() 会把进程打死。本仓库已单例化并绕开热路径。
