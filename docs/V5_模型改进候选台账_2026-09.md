# V5 模型改进候选台账（2026-09-01 起，随实验滚动更新）

> **性质**：本台账登记"已有实验证据表明可能影响生产模型（V3.7/V3.8 评分与执行层）的候选改动点"。
> **纪律**（源自 `优化计划_V5_重规范_2026-09.md` §0）：
> 1. 本台账中一切现有证据均为 **SURV-ADJ 口径**（幸存池），只作差异量化与候选登记，**不作最终裁决**；
>    任何候选须完成对应预登记实验并通过 §0.3 统计门，最终在 FULL-PIT（A5 阶段）复核后才允许进生产。
> 2. 双向裁决：**维持现状与改动同为合法结论**；证据失效 ≠ 证明有害，只证明"原依据不能再作为辩护"。
> 3. 每条候选 = {改动内容, 证据链（实验编号→产物→统计量）, 当前判定, 下一步动作}。禁止倒果为因、禁止夹带未登记改动。

---

## 索引（2026-09-02 更新）

| 编号 | 主题 | 当前判定 | 类型 |
|---|---|---|---|
| C1 | 回撤惩罚项（F_mdd）去留 | 双门未全过 → **维持现状**；列为 FULL-PIT 后**第一批复核（最高优先）** | 评分改动 |
| C2 | 动量平滑 M2 替 M1 | 双门皆堵 → **否决 M2，维持 M1** | 评分改动 |
| C3 | alpha_ir 权重/独立性 | 依据弱化告警，**非移除证据**；A5.1 终审 | 评分改动 |
| C4 | 低波因子 ntuw252 增量 | HAC 后仍显著的**候选增量**（中周期信号）；A5.1 终审 | 评分改动 |
| C5 | 执行口径修复（复权+T+1+阶梯成本） | **已完成并封口**，R3 = SURV-ADJ 唯一权威基线 | 口径（已落地） |
| C6 | V5-全平滑 | 参考档，**不入产**（多处同改，不可归属） | 评分改动 |
| C7 | 模型动物园 ML 腿 | **无统计区分 → 维持 V3.7，不引入 ML 腿** | 评分改动 |
| C8 | 标签窗前视硬断言 | **建议采纳**（零数值风险的工程护栏） | 护栏 |
| C9 | ML 投入排序：先扩样本后调模型 | 方法论判定，**非对 ML 的否决** | 排序 |
| C10 | 平滑择腿（value 排除在外） | 弱证据、不入产，仅为将来设计留向 | 评分改动 |
| C11 | 执行层参数封口 | **禁止再调**（平坦区中上位，过拟合风险主导） | 参数（已封） |

> 类型说明：「评分改动」须走完整预登记 → 冻判门 → FULL-PIT 终审；
> 「护栏 / 排序 / 口径」不改变任何既有数字，不适用 §0.3 统计门。

---

## C1 — 回撤惩罚项（F_mdd / P1-4）去留判决【最高优先】

- **改动内容**：生产评分 S 中含回撤惩罚分量；候选为**移除或保留**该惩罚项。
- **证据链**：R3.2（预登记 #23，产物 `output/v5/r32_p14_penalty_hac.csv`）：
  - P14_diff_SwithoutMdd-S fwd6：IC 差 −0.0143，naive t=−2.21 → **HAC t=−1.25（失效，翻转项 3/26 之一）**。
  - 历史采纳依据`去掉 MDD 惩罚显著更差（t≈−2.21）`在 HAC 门下不再成立。
- **当前判定**：**证据真空**——维持现状仅为默认，不可再引用 −2.21 为辩护；亦不能反推"移除更好"。
- **#37 已执行节果（2026-09-01, c_experiments.py, M11 后权威面板 145 月/52,152 行）**：
  H1: OOS(2024-06+) fwd6 配对 IC 差 HAC t = **+1.11（未过 2）**
    （全期 t_naive −2.38 → HAC −1.33，方向仍偏负→即全期样本里面板视角「摘惩罚更差」,与 OOS 正收益并不矛盾）；
  H2: 端到端 A_ref +10.84% → C1B **+12.20%（Δ+1.36pp）**, MaxDD −18.5%→−18.4%(Δ−0.01pp 合格 ≤2pp),
    Calmar 不降——H2 过。
  **双门未全过 → 维持现状**（预登记否决条款生效）。
  亮点信号（OOS fwd6 IC 方向翻正 +1.11 / 端到端 +1.36pp）属「松他化趋势」证据,不构成显著改进；
  **若 U4.1 FULL-PIT 面板下重跑仍然 OOS 动量方向为正,可申请重开评断**——现案不采。
- **【2026-09-02 追加·复核优先级登记】**：本条列为 **FULL-PIT 打通后第一批复核项（优先级最高）**。
  理由（基于 C1 实测数字，非直觉）：移除 MDD 惩罚后 CAGR **+1.36pp** 而 MaxDD 仅动 **−0.01pp**
  ⇒ 在 SURV-ADJ 幸存池里该惩罚项**没有兑现风控价值，却在持续吃收益**。
  但**幸存者偏差恰恰会系统性低估回撤**（清盘/巨亏基金被剔出池），全池口径下该惩罚的风控价值
  很可能被重新证实——**也可能被彻底否定**。两个方向都是合法结论，故这是全部候选中**信息量最高**的一次复核。
  本条不改变现判定（维持惩罚项），只登记复核排序。

## C2 — 动量平滑 M2（V3 变体）入产判决【~~正向增量候选·第一名~~→已裁决：否决，维持 M1】

- **改动内容**：生产评分中 F_momentum 采用 M1 平滑（V2）；候选为换用 **M2 平滑（V3 变体）**。
- **证据链**：R3.4（预登记 #23，`output/v5/r34_adoption_chain_hac.csv`）：
  - V3−V0 配对差：IC 差 **+0.0090**，naive t=2.54，**HAC t=2.69（46 有效季，L=2，显著维持）**；
  - 但历史采纳链中该变体**未进生产**（当年无显式否决记录，M2 注记成立）。
  - 对照：已采纳的 V1（HAC 2.57）、V2（HAC 2.79）证据强度与 V3 同级。
- **#38 已执行节果（2026-09-01, c_experiments.py, 同上通道）**：
  H1: OOS fwd6 配对差 HAC t = **−0.47**（方向反了：+0.0090 的历史正向并未跨到 OOS）；
    ALL 段 t_naive −2.21/HAC −2.55——全期仍採配对差为负,历史 R3.4 的 +0.0090 系数（R3.4 季粒度 L=2）
    被月粒度 OOS 渠道证反转；**R3.4 的 V3“显著”本身即 SURV-ADJ 季粒度证据，环境再反转属典型**。
  H2: 端到端 CAGR −1.45pp / MaxDD 劣化 **+4.3pp（远超 2pp 上限）**, H2 断过。
  **双门皆堵 → 维持 M1（否决 M2）**。
  教训：报表正 IC 信号（V3 在季粒度 OOS t=2.69）不能直接折算到端到端——惩罚/执行
  的耦合又吞取理论增益，「WL 性解耦」的代表；此结论同步修正上段 RHS「证据已显著却未应用」表述为
  「报告级显著。**跨通道端到端型鉴定：D0.6 计数规则居然捕获真实组合层面的塌缩证据**」。
- **下一步**：无新实验——若用户日侧补 2025-04+ 净值后 U4.1 PIT 面板下重跑仍然 M2 更强,才准重开。

## C3 — alpha_ir 因子权重/独立性复核【依据弱化告警】

- **改动内容**：复核生产评分中 alpha_ir（inverse-vol 调整 alpha）分量的权重与"独立 alpha"引用方式。
- **证据链**：R3.1（`output/v5/r31_factor_ic_hac.csv` / `r31_subgroup_ic_hac.csv`）：
  - 全样本 alpha_ir fwd6：naive t=2.41 → **HAC t=1.30（失效）**；ashare 子群 2.69→1.52 同步失效；
  - 海外子群 alpha_dc fwd6 2.06→1.25 同样失效。
  - 注意：fwd1/fwd3 未翻转，失效集中在 **fwd6 独立预测力**这一引用面。
- **当前判定**：**依据弱化，不构成"移除"证据**；权重是否应降/因子族是否需重构属开放问题。
- **下一步**：A5.1（FULL-PIT 因子体检）中全 horizon 终审；在之前，README/报告中凡引用 alpha_ir 独立 alpha 处
  按 F7.2 勘误降级措辞（已列入勘误执行清单）。

## C4 — 低波因子 tuw252 作为增量候选【正向·待终测】

- **改动内容**：将低波因子（ntuw252：非重叠低波窗口变体）作为评分增量分量入产。
- **证据链**：R3.3（`output/v5/r33_p22_hac.csv`）：
  - ntuw252 fwd6 IC +0.0505，naive t=4.13 → **HAC t=2.33（存活，修正历史台账 3.97 的朴素口径）**；
  - fwd1 翻转失效（2.04→1.92）；fwd3 存活（3.65→2.87）；
  - P2-A 体制条件（价值体制门控）配对差均不显著→否决维持。
- **【2026-09-02 追加】** 本条是 R3.1–R3.4 全部 HAC 复核中**唯一稳健存活的新增因子线索**：
  fwd3 t_HAC 2.87 / fwd6 t_HAC 2.33 双 horizon 存活，而 fwd1 翻转（2.04→1.92）
  ⇒ 该信号的有效期在**中周期**，不是短周期择时信号。此性质须在正交残差检验中一并保留。
- **当前判定**：**修正口径后仍显著的候选增量**；H-P2E 待复测项已改登记至 A5.1 的 FULL-PIT 面板执行。
- **下一步**：A5.1 终审；若通过 §0.3 门，再入"因子增量正交残差 HAC t>2"检验（防与 vol126/tuw252 同源重复计分）。

## C5 — 执行口径修复（已完成，非模型改动但改变一切基线数字）

- **内容**：成交价复权（D0.2）+ T+1 成交（D0.4）+ 阶梯赎回成本 + 全收益基准（D0.3 待用户侧）。
- **证据链**：B2.1（`output/v5/b21_decomposition.csv`，**M11 修复后基准**）：
  R0 +5.08% → **+复权 +3.51pp（=+8.59%）** → +T+1 **+2.14pp（=+10.73%）** → 阶梯成本 **+0.11pp**
  → **完成态基线（R3）CAGR +10.84%, MaxDD −18.5%, Calmar 0.59**；R4 全收益基准列仍 TR_PENDING（待 d03 用户侧）。
  并列窗口漂移补强证据：B2.x 轮间版差 ±0.6pp（M11 根因勘正文 §3），本条数字为确定性冻结面板上的唯一权威。
- **应用点**：一切后续实验的模拟器一律 sim_core（复权+T+1 口径默认）；对外数字以 R3 行为 SURV-ADJ 唯一基线
  （+10.84%，非此前简报引用的 +8.69%）；旧 6.16% / raw 口径数字封存为历史档案（F7.3 凡例执行项）。

## C6 — V5-全平滑（参考档，不入产）

- **证据链**：R3.4：V5−V0 配对差 +0.0176，HAC t=2.34（参考级）。同时改动 alpha+mom+value 三处，
  不满足"单一改动可归属"的采纳条件 → 仅作参考，不入候选。

## ⏳ 待出证据的候选（阻塞中）

- ~~**R3.5 模型动物园**~~ → **已出证据（2026-09-01 第三轮）**：见下 C7。
- **B2.1-R4 全收益基准**：D0.3 解锁后补 TR 列，基线外的基准相对值 +~2pp/yr 档案量级修正。
- **U4/A5（FULL-PIT）**：一切候选的最终裁决口径；d01/d03 用户侧产物就绪前冻结。

---

## C7 — 模型动物园（R3.5）修复后结论：ML 腿不构成候选【无统计区分，维持现状】

- **改动内容**：是否在生产评分中引入 ML 分散腿（历史报告 §4 曾建议 ElasticNet/Lasso F2 fwd6_rk ×0.3）。
- **证据链**：R3.5（预登记 #24，产物 `output/v5/r35_zoo_redo/{ic_table,pairdiff,fwd12_ab}.csv`）：
  - 口径：SURV-ADJ **半池**（种子池 217 只中净值缓存覆盖 110 只）、12,383 行 × 138 月末、冷启动对齐起点 2016-04-30；
  - **自身 IC（HAC）**：V3.7 +0.1011（**3.32** 显著）；lasso F2 +0.0550（1.36）、enet F2 +0.0533（1.29）、
    svrlin F2 +0.0640（1.63）、lasso F3 +0.0473（1.06）、huber F2 fwd6_z +0.0584（1.43）——**5 个 ML 全部不显著**；
  - **配对差 V3.7−ML（HAC，§0.3 变体门）**：full 1.10~1.43、w2019 1.48~1.72 —— **全部未过 2**。
- **当前判定**：**三种合法结论中落"无统计区分"一档**。
  - 不能宣称"ML 反超"（ML 自身 IC 均不显著、配对差方向全为 V3.7 正）；
  - **也不能宣称"V3.7 显著优于 ML"**——历史报告"双冠/全部领先"式表述在 HAC 配对差门下**无证据支撑**
    （naive 口径 2.09~3.11 看似显著，HAC 后 1.10~1.72 全失效）。正确表述：**台账内无法证伪现状 ⇒ 维持 V3.7，不引入 ML 腿**。
  - 与审计 §2 对"V3.7 优于全部 66 个 ML 配置"的裁决（"方向有效，强度需降格"）一致，且本轮把降格幅度**量化**了。
- **F4 爆炸半径实测确认（本轮最硬的一条）**：huber F2 fwd12_z 把训练截止由硬编码 q−6M 改为 h 匹配 q−12M 后，
  IC **+0.0773（naive 4.20）→ −0.0842（naive −3.80 / HAC −2.00）**，符号翻转；V3.7−该配置配对差 HAC **3.36** 显著。
  ⇒ 审计 F4 判断（该配置的历史成绩由偷看未来 6 个月的标签支撑，且爆炸半径限于单一配置）**被实测证实**。
- **披露（不利于本结论强度，如实登记）**：
  1. 半池（110/217）+ 幸存池并集口径 → 本条仅为**勘误级**证据，非 FULL-PIT 裁决；
  2. w2015 窗因冷启动起点（2016-04）晚于其门槛（2015-01）**塌缩为 full 窗同一样本** ⇒ 本轮实为 full/w2019 双窗；
  3. **端到端（sim_core 复权+T+1+阶梯成本）三窗口表尚未补出** → #24 步骤 6 未完成，C7 现为"IC 层结论"，
     端到端层结论待补；在补出前不得宣称模型级双门已过。
- **下一步**：A5.2（FULL-PIT 动物园 2.0）终审；在此之前 README/报告凡引用"V3.7 双冠/全部领先/t=8.11"处按 F7.2 勘误降级。

---

## C8 — 标签窗前视断言（护栏类改动，非评分改动）【建议采纳·零数值风险】

- **改动内容**：在 `_build_ml_panel.py` 及一切打标脚本入口加**硬断言**——标签起点必须 ≥ 特征快照日 + 执行延迟；
  并把 `{标签窗, 特征窗, 训练截止规则, 执行延迟}` 四元组写入每份产物的 manifest 头。
- **证据链（本轮最硬的一条实测）**：R3.5 / `output/v5/r35_zoo_redo/fwd12_ab.csv`：
  `huber F2 fwd12_z` 仅把训练截止由 q−6M 改为 h 匹配的 q−12M，IC **+0.0773 → −0.0842**（符号翻转，HAC −2.00）。
  ⇒ **一处标签窗前视足以把"看似全场最佳"的配置翻成显著为负**。
- **当前判定**：**建议采纳**。本条不改任何评分/权重/执行参数，对所有既有数字**零影响**（纯增加失败模式检出），
  故不适用 §0.3 统计门；属工程护栏而非模型改动。
- **下一步**：与 M12（测试静默 skip → 显式 FAIL）合并为一个"研究纪律护栏"批次实施；实施后补跑 #16 测试断言。

## C9 — ML 腿投入排序：先扩样本，后调模型【方法论判定·非模型改动】

- **内容**：暂停 ML 超参/特征工程投入，直到 FULL-PIT 全池面板（#26）就绪后再重跑 R3.5。
- **证据链**：C7 实测——5 个决赛配置 t_HAC 落在 **1.06~1.63**，配对差 t_HAC **1.10~1.72**。
  关键读法：这是**"样本不足以区分"**的证据，**不是**"ML 无效"的证据。当前样本仅 110 只 / 12,383 行 /
  共同冷启动 2016-04（有效 123 个月末），在此样本量下即便真实存在 IC 差也难以过 HAC 门。
- **当前判定**：**在 FULL-PIT 前继续调 ML 属于在噪声里挖矿**；登记为投入排序判定，不构成对 ML 的否决。
- **下一步**：#26 U4.2 全池面板落地后重开 A5.2；届时样本量预计增加约一个数量级。

## C10 — 平滑变体择腿（value 腿排除在平滑之外）【弱证据·待 FULL-PIT】

- **改动内容**：若未来采纳平滑类改动，应做 **mom + alpha 双腿平滑**，而**不做** C6 式"全平滑"。
- **证据链**：R3.4（`output/v5/r34_adoption_chain_hac.csv`）配对差 HAC：
  mom 平滑 M1 **+0.0151（2.79）**、alpha 平滑 **+0.0091（2.57）**、**value 平滑 −0.0035（−1.08，方向为负）**、
  全平滑 +0.0176（2.34）。⇒ 全平滑的增益基本来自 mom+alpha 两腿，value 腿在拖累。
- **当前判定**：**弱证据、不入产**。C6 已因"多处同时改动不满足单一可归属"被排除；本条只是把 C6 的排除理由**细化到腿级**，
  为将来重开时的设计提供方向，**不构成采纳依据**（幅度仅 1~2pp IC 量级，且为 SURV-ADJ 季粒度）。
- **下一步**：A5.1 FULL-PIT 因子体检时按单腿分别登记，禁止打包提交。

## C11 — 执行层参数封口【已封·登记为"禁止再调"】

- **内容**：执行层（slots/buy/sell/CPPI/危机/止损/再平衡）**停止调参**。
- **证据链**：① B2.3 延迟敏感性 delay=0/1/2 → CAGR **+9.55% / +10.84% / +10.74%**（非尖峰，T+1 不是甜点参数）；
  ② S6.2 243 格邻域 → 基线分位 **82%**，CAGR 全域 **[+5.43%, +12.53%]**，参数位于平坦区中上位。
- **当前判定**：**继续调参的期望收益低于过拟合风险**。C5 已封 B2.1 R3 为唯一权威基线口径；本条把"禁改"从口径层
  扩展到**参数层**。注意 S6.1/S6.2 均为 `仅报告不平判` 自封项，**其结果本身也不得用于调参**。
- **下一步**：无。任何执行层参数改动须先立新预登记并说明为何推翻本条。

---
*维护规则：每项结论入台账前必须能指到产物路径 + 统计口径 + 有效窗；任何"应该更好"的直觉一律不入账。*
