# V5 产物重建 + R3.5 + S6.1 执行计划与进度台账（2026-09-01 起，第三轮沙箱）

> **本文性质**：接续 `优化计划_V5_重规范_2026-09.md`（主契约）的**执行层工单**，不新增任何实验项、不修改任何已冻结的判定门。
> 本轮全部工作属于以下三类，**没有第四类**：
> 1. **产物重建**（第二次沙箱重置导致 `output/*`、`cache/navadj_*`、venv 全损；结论数字已固化在文档中，缺的是底层实物）；
> 2. **已登记未完成项的执行**（R3.5 = 预登记 #24；S6.1 = 预登记 #33）；
> 3. **不变量测试常绿**（M1.1 闸门）。
>
> **铁律（本文自我约束，违反即产物作废）**
> - 重建产物的验收标准 = **与文档已固化数字逐位/逐项复现**；若复现不一致，**不许改文档去迁就新数字**，必须立案（新 M 编号）查根因。
> - R3.5 / S6.1 属**未出结果**的实验：**不预设方向**。ML 反超、V3.7 维持、两者无统计区分三者均为合法结论；S6.1 敏感/不敏感同为合法结论。
> - S6.1 与 S6.2 均为 `仅报告不平判` 自封项：**禁止据其结果调参**。
> - 一切产物打口径标签 **SURV-ADJ**（幸存池）；本轮不产生任何 FULL-PIT 裁决证据（D0.1/D0.5 仍受阻）。
> - 每完成一批立即 `git commit`；产物本体留在 `.gitignore` 外（`output/`、`cache/`），仓库只进 **manifest 指纹 + 文件清单 + 报告**。

---

## 0. 环境与前置（阶段 E）

| 步骤 | 内容 | 验收标准（预登记） | 状态 |
|---|---|---|---|
| E-1 | 重建 venv：`python3 -m venv /home/user/.venv_review` + `pip install -r requirements-v5-lock.txt requests py_mini_racer` | 钉版一致：pandas 3.0.5 / numpy 2.4.6 / sklearn 1.9.0 / scipy 1.17.1 / statsmodels 0.15.0 | ✅ 已完成（实测三项主版本逐一吻合） |
| E-2 | 核实资产存活（铁律 6：先 `git log`/`ls` 取证再行动） | `cache/nav_*` = 15,444；git 历史完好至 `ec2ee7d`；`output/` 仅剩 2 个 git 跟踪文件 | ✅ 已完成（15,444 ✓；HEAD=ec2ee7d ✓；`cache/navadj_*`=0 确认全损） |
| E-3 | 硬件预算勘定（影响排期，非实验项） | 记录核数/内存，据此重估 S6.1 的 ~15h CPU 预算 | ✅ 已完成：**2 核 / 3GB RAM / 18GB 可用磁盘** → S6.1 需按 2 核串行重估（见 §3） |

---

## 1. 阶段 A —— 产物重建（最高优先，下游一切依赖）

**重建链**：`build_navadj.py` → `p1_panel_build.py`（canonical 面板）→ {R3.1–R3.4, B2.1, B2.2/B2.3, S6.2, C1/C2} → manifest。

### A-1 D0.2 复权序列重建 ✅ 已完成

- **命令**：`/home/user/.venv_review/bin/python build_navadj.py --workers 2`
- **预登记验收**（对照 `优化计划_V5` §1 D0.2 行 + `V5_复跑事故` §1 灾后指纹）：

| 指纹项 | 文档已固化值 | 本轮实测 | 判定 |
|---|---|---|---|
| 研究池 universe | 4,987 只 | **4,987** | ✅ 一致 |
| adj 构建成功数 / 错误 | 4,987 / 0 | **4,987 / 0** | ✅ 一致 |
| 事件日总数 | 21,454 | **21,454** | ✅ 一致 |
| 事件分类（分红回补/数据异常/上拆） | 12,090 / 9,248 / 116 | **12,090 / 9,248 / 116** | ✅ 一致 |
| 覆盖基金数 | 3,494 | **3,494** | ✅ 一致 |
| raw vs adj 年化差 均值 / P95 / max | +0.6215% / +3.91% / +78.78% | **+0.6215% / +3.91% / +78.78%** | ✅ 一致 |

- **结论（客观）**：D0.2 产物**逐项复现**，无一项偏离。`cache/navadj_*.csv` 4,987 份、`output/v5/d02_*` 已恢复。
- **意义**：确权了"结论数字没丢、只丢实物"这一判断在 D0.2 上成立；同时证明 venv 钉版重建未引入数值漂移。

### A-2 M1.1 不变量闸门复验 ✅ 已完成

- **命令**：`/home/user/.venv_review/bin/python -m pytest tests/test_research_invariants.py -q`
- **预登记验收**：28 passed / 0 fail（文档 M1.1 行固化值）。
- **实测**：navadj 重建**前** 27 passed / **1 skipped**（skip 项 = 复权真值锚点用例，因 `cache/navadj_*` 缺失而跳过）；
  navadj 重建**后** **28 passed / 0 fail** → ✅ 与文档一致。
- **附带发现（登记，不裁决）**：该测试套件对"产物缺失"是 **skip 而非 fail**，即在产物全损时闸门会**静默降级**。
  这是 M10 修复的一个残留缺口，登记为 **M12（闸门降级不可见）**，处置建议留 F7 阶段，本轮不擅自改测试语义。

### A-3 canonical 面板重建 ✅ 已完成

- **命令**：`p1_panel_build.py --start 2013-01 --end 2026-03 --maxn 500 --workers 2`（`--seed-salt` 缺省 0 = 历史原式）
- **预登记验收**：159 个决策月全落盘；逐月合格计数与文档固化值一致（**2013-01 = 1 … 2026-03 = 3,956**）。
- **实测**（耗时 2,316s / 2 核）：

| 指纹项 | 文档已固化值 | 本轮实测 | 判定 |
|---|---|---|---|
| 决策月枚举 | 159 | **159**（首月 2013-01 合格=1 无有效评分，属原式） | ✅ |
| 2013-01 合格数 | 1 | **1** | ✅ |
| 2026-03 合格数 | 3,956 | **3,956** | ✅ |
| 有效面板月数 × 行数 | 145 月 / 52,152 行（C 台账 #37 段） | **145 月 / 52,152 行** | ✅ |
| 面板区间 | 2014-03 → 2026-03 | **2014-03-31 → 2026-03-31** | ✅ |
- **⚠️ 关于用户工单中的"修好 `--panel-dir`/`--seed-salt`"**：经逐行审读，两参数在 `ec2ee7d` 中**已存在且实现正确**
  （`--panel-dir` 经 `_set_panel_dir` 改写全局并建目录；`--seed-salt` 只作用于抽样种子且 `salt=0` 严格退化为历史式 `seed=int(YYYYMMDD)`）。
  故本项**无需修复**——如实登记为"工单预期与代码现状不符，以代码为准"，不做无谓改动（避免夹带未登记变更）。
  已识别的**真实**待办见 A-3b。

### A-3b 面板构建器的两处待登记缺陷（只登记，先不改）

| 编号 | 现象 | 影响面 | 处置 |
|---|---|---|---|
| M13 | `build_date()` 内 `ThreadPoolExecutor(max_workers=2)` **硬编码**，`--workers` 入参被静默忽略 | 不影响数值（M11 修复后按 code 稳定排序，结果与线程数无关）；只影响吞吐 | 本轮**不改**（改动会影响 S6.1 与 canonical 的运行条件对称性）；登记待 F7 |
| M14 | `build_universe()` 优先读 `universe.csv` 缓存，缓存在 `--panel-dir` 变体目录下会被**各自重建** | S6.1 变体面板会各自生成 universe.csv；因输入同源（同一批 nav 文件）内容应一致 | 本轮验证其内容 sha256 与 canonical 一致后即可放行；不一致则立案 |

### A-4 下游统计复跑（面板就绪后执行）⬜ 待开始

按依赖顺序，逐项对照文档固化数字验收：

| 步骤 | 脚本 | 预登记验收（文档固化值） | 状态 |
|---|---|---|---|
| A-4.0a | `p2_vol_build.py`（**新发现的前置**，r33 依赖 `output/p2/vol_panel.csv`） | 52,152 行、vol126/vol252/tuw252 全非空 | ✅ 实测 52,152 行、三列各 52,152 非空 |
| A-4.0b | `factor_study.py`（**新发现的前置**，r34 依赖 `output/factor_variant_rows.csv`） | V0 IC **0.1212** / t **4.49**；V0复建 vs 引擎S 相关 **0.9765** | ✅ 逐项一致（相关 0.9765、最大差 40.0） |
| A-4.1 | `r31_hac_redo.py` | 全样本翻转 **4/39**（value fwd3 −2.16→−1.67、alpha_ir fwd6 2.41→1.30、mom4 fwd3 2.56→1.91、eng_value fwd3 −2.24→−1.72）+ 子群翻转 **6/35** | ✅ 4/39 + 6/35，四条 t 值逐位一致 |
| A-4.2 | `r32_hac_redo.py` | 翻转 **3/26**；S1 正交 fwd3 −2.56→−1.91、fwd6 −3.12→−1.95；P1-4 MDD 惩罚 −2.21→**−1.25** | ✅ 三条逐位一致（S2 fwd6 −4.05→−2.45 存活亦同） |
| A-4.3 | `r33_hac_redo.py` | 翻转 **1/41**（ntuw252 fwd1 2.04→1.92）；ntuw252 fwd6 HAC **2.33** 存活、fwd3 3.65→2.87 | ✅ 逐位一致（fwd6 IC +0.0505）；P2-A 配对差全不显著维持 |
| A-4.4 | `r34_hac_redo.py` | **0 翻转**；S_eng−V0 **3.32**、V1−V0 2.57、V2−V0 2.79、V3−V0 2.69、S4−V0 −1.08、V5−V0 2.34 | ✅ 六条逐位一致，flip 全 False（46 有效季） |
| A-4.5 | `b21_baseline.py` | 分解 R0 **+5.08%** → R1 **+8.59%** → R2 **+10.73%** → R3 **+10.84%**；trades 119/73/93/93；MaxDD −18.5~−18.9% | ✅ 四档 CAGR + 交易笔数 + MaxDD(−22.4/−18.7/−18.9/−18.5) 全一致 |
| A-4.6 | `b22_b23.py` | B2.2：27 格 CAGR ∈ **[+7.08%, +11.07%]**、中位成本 ¥15,801；B2.3：delay 0/1/2 → **+9.55%/+10.84%/+10.74%**（82/93/105 笔） | ✅ 全部逐位一致 |
| A-4.7 | `s62_param_grid.py` | 243 格 CAGR ∈ **[+5.43%, +12.53%]**；基线分位 **82%**；buy 线 median 9.9/9.8/7.6% | 🔄 运行中 |
| A-4.8 | `c_experiments.py` | C1：OOS t **+1.11**、端到端 +10.84%→**+12.20%**、MaxDD −18.5→−18.4；C2：OOS t **−0.47**、CAGR −1.45pp、MaxDD 劣化 **+4.3pp** | ⬜ |
| A-4.9 | `p4_alpha_decay.py` + `rbsa_ew_study.py` → `m12b_hac_remainder.py` | HP4A-3 OOS 中间门 naive 2.50 → HAC **1.61**（翻转）；S3r **2.06** 擦线存活；rbsa_ew n=10 < 3L=15 标注 | ⬜ |
| A-4.10 | `v5_manifest.py` 全批回补 | `output/v5/manifest.jsonl` 覆盖本轮全部产物 | ⬜ |

**不一致处置规则（预登记，先于结果写死）**：任一项与固化值不符 →
(a) 先查是否为 M11 类**非确定性**（重跑两次看是否自复现）；(b) 再查环境/输入哈希；
(c) 仍不符则**以新编号立案**并在主契约状态表追加"复现失败"行，**禁止**静默改写文档旧值。

---

## 2. 阶段 B —— R3.5 模型动物园（预登记 #24）⬜ 待开始

- **前置**：`output/bt_scores_cache/*_2e4ec0f5.csv` 已随重置丢失 → 需先跑 `r35_rebuild_zoo_cache.py`
  （种子池 216/217 只，2014-04→2026-03 共 144 个月末；文档记录第一轮覆盖率 **110/217**，本轮须复核该数）。
- **步骤**：
  1. ⬜ `r35_rebuild_zoo_cache.py`（断点续跑，CPU 密集）→ 验收：覆盖率与首轮一致；
  2. ⬜ `_build_ml_panel.py` → `output/ml_panel.csv`；验收：F4 修复仍在位（**无** `nav_last ≥ date+60d` 后向过滤行）；
  3. ⬜ `r35_zoo_redo.py` → 注意工单点名的 **`trend_ok01` 派生列**问题，运行前先核对该列构造是否已修好；
  4. ⬜ 出报告勘误段（`output/model_zoo_report.md` 追加修订段，**原文保留**）。
- **判定门（沿用 #24，不改）**：V3.7 vs 各 ML 一律报**配对差 HAC t**；冷启动统一对齐；fwd12 训练截止 q−12M。
- **不预设**：H0（V3.7 维持）与 H1（ML 反超）同为合法结论。

---

## 3. 阶段 C —— S6.1 种子×maxn 网格（预登记 #33）⬜ 待开始

- **网格（本轮落地子集，已在 `s61_runner.py` 冻结）**：种子 {1..5} × maxn {500,1000} + 全取 ×1 = **11 组面板**。
- **参照系维度（批内 rank vs 全市场 ECDF）本轮不做**：依赖逐月全宇宙动量参照快照（须 U4.1 宇宙底座重造）——
  此为**如实披露的缺口**，不得宣称 #33 已全维完成；本轮报告标题必须写明"（#33 子维度）"。
- **CPU 预算重估**：canonical 单组 159 月在 2 核上耗时 T；11 组变体中 maxn=1000 与全取组显著更重，
  故 ~15h 的原估计在 **2 核**机器上需重新标定（不臆测，以 canonical 实测 T 外推后写回本表）。
- **步骤**：1. ⬜ `s61_runner.py` 后台分箱串行（断点续跑安全）；2. ⬜ `s61_summarize.py` 出 `output/v5/s61_robustness.csv` + `s61_summary.md`。
- **自封条款**：仅报告组间 IC 均值/HAC t 分布，**不平判、不调参**。

---

## 4. 进度日志（只追加，不修改）

- **2026-09-01 T0**：读毕主契约 + 审计报告 + C 台账 + 事故复盘 + P1/P2 勘误段；核实资产（nav 15,444 存活 / navadj 全损 / git 完好）。
- **2026-09-01 T1**：E-1 venv 重建完成（钉版吻合）；E-3 硬件勘定 2 核/3GB。
- **2026-09-01 T2**：A-1 D0.2 重建完成 —— **6 项指纹逐项复现，零偏离**。
- **2026-09-01 T3**：A-2 不变量闸门 **28 passed / 0 fail** 复现；登记新缺陷 **M12（产物缺失致测试静默 skip）**。
- **2026-09-01 T4**：A-3 canonical 面板构建启动；审读确认 `--panel-dir`/`--seed-salt` **本已正确**（工单预期与代码现状不符，以代码为准）；登记 **M13/M14** 两处真实缺陷（本轮只登记不改）。
- **2026-09-01 T5**：A-3 canonical 面板重建完成 —— 159 决策月 / **145 月 × 52,152 行**，首末合格数 1 / 3,956 与固化值一致（2,316s @2 核）。
- **2026-09-01 T6**：A-4.0a/0b 两处**新发现的前置产物**补建（`p2_vol_build.py`、`factor_study.py`）——
  它们不在原工单清单中，但 r33/r34 硬依赖；factor_study 复现 V0 IC 0.1212/t 4.49/相关 0.9765，与灾后指纹一致。
- **2026-09-01 T7**：A-4.1–A-4.6 全部复现成功，**零偏离**：R3.1（4/39+6/35）、R3.2（3/26 关键三条）、R3.3（1/41）、
  R3.4（0 翻转，六条配对差）、B2.1（+5.08/+8.59/+10.73/+10.84，笔数 119/73/93/93）、B2.2（[+7.08%,+11.07%]，中位 ¥15,801）、
  B2.3（+9.55/+10.84/+10.74，82/93/105 笔）。**至此"结论数字未丢、仅实物丢失"的判断在 A 链上被逐项证实。**
- **2026-09-01 T8**：A-4.7/4.8/4.9 复现成功：S6.2（243 格 [+5.43%,+12.53%]、基线分位 **82%**）、
  C1（OOS t_HAC **+1.11**、端到端 +10.84%→**+12.20%**、ΔMaxDD −0.01pp → 双门未全过 → 维持现状）、
  C2（OOS t_HAC **−0.47**、ΔCAGR −1.45pp、ΔMaxDD **−4.31pp** → 双门皆堵 → 维持 M1）、
  m12b（HP4A-3 OOS 2.50→**1.61** 翻转、S3r 3.03→**2.06** 存活、rbsa_ew n=10 标注）。
  **A 链 10 项全部复现，累计零偏离** —— 预登记的"不一致处置规则"本轮未被触发。
- **2026-09-01 T9**：A-4.10 manifest 回补（`output/v5/manifest.jsonl`）+ 产物清单落库
  `docs/artifacts/第三轮产物清单_2026-09-01.md`（navadj 4,987 份 + output 全量路径/字节/sha256-16）。
- **2026-09-01 T10**：⚠️ **GH_TOKEN 在会话中途失效**（首次 push 成功，其后 `git push`/`gh` 均报 authentication failed）。
  按事故复盘 §4 的教训，本地 commit **不具备抗回滚能力** → 已书面提示用户重连 GitHub；在此之前所有新 commit 仅存于本地，风险敞口如实登记。
- **2026-09-01 T11**：阶段 B 启动：`r35_rebuild_zoo_cache.py` 运行中，种子池 **217 只 / 净值覆盖 110/217**（与第一轮一致 ✅），144 个月末待打。

---

## 5. ⚠️ 第三次沙箱重置（2026-09-01 T12）—— 事故登记

**事实（按铁律 6 先取证后行动，取证命令与结果如实记录）**：

| 取证项 | 结果 |
|---|---|
| `git log --oneline -1` | 回到 **ec2ee7d**（本轮两个本地 commit `575b1e2`/`6ef66b3` 灭失） |
| `.git` 形态 | 被替换为新 clone（无 upstream 跟踪、无本地分支历史）——**与第二次重置同型** |
| `ls cache/nav_*` | **15,444 存活**（原始净值缓存三次重置均未受损） |
| `ls cache/navadj_*` | **0**（4,987 份全损，第二次） |
| `output/p1_panel/*.csv` | **0**（145 月面板全损） |
| `/home/user/.venv_review` | 不存在（第二次重建后再次灭失） |
| `/tmp/*.log` | 全损（运行日志不可追溯） |
| 远端 `origin/arena/...` | **6d58c35 存活** ✅ —— 唯一幸存的账本 |
| 未跟踪的 `docs/*` 新文件 | **存活**（工作区文件快照层保住了计划文档与产物清单） |

**净损失**：A 链 10 项的**产物实物**（约 1.5h CPU）+ 两个本地 commit 的台账文字（**已由磁盘存活的文档全文恢复**）。
**净未损失**：A 链 10 项的**验收结论**（全部逐位复现、零偏离）——因为它们在灭失前已写入磁盘上的本文件。

**教训兑现（第三次印证同一条）**：`V5_复跑事故与M11发现` §4 的判断再次被证实——**本地 commit 在回滚面前等于零，只有已 push 的远端引用幸存**。
本轮 T10 已识别 GH_TOKEN 失效并书面提示，但在用户重连前的窗口期内发生了重置，因此损失落地。
**自本条起执行更强纪律**：每个工作单元完成 → `git commit` → **立即 `git push`；push 失败即视为"该批次未完成"并停止推进新的长任务**，
优先解决推送通道，避免再次用 CPU 时间换取一个不可保全的产物。

**处置**：不改任何已验收数字（它们是磁盘存活的原始记录）；A 链产物按需重建（脚本与验收标准均已固化，重建是确定性的、可无人值守的）。

---

## 6. 阶段 B（R3.5，预登记 #24）执行记录 ✅ 已完成（IC 层）

### B-1 zoo 评分缓存重建 ✅

- **提速基建（纯工程，零语义变更）**：新增 `r35_zoo_cache_shard.py`。
  依据：实测单进程 CPU 占用仅 ~40%（**I/O bound**，瓶颈在净值 CSV 反复读取），2 核串行需 ~5.5h。
  各月末评分彼此独立 + M11 后 `finalize` 按 code 稳定排序 ⇒ 可安全按月分片。
  **实测提速**：串行 3 月/7min → 3 分片 24 月/7min，全量 144 月约 45min。
- **一致性自证（不靠推理，靠实测）**：`--verify 2020-06-30` 重算并与落盘产物比 sha256 →
  `c23979c1b9c4c062` vs `c23979c1b9c4c062` **一致 ✅**（分片与串行产物等价）。
- **覆盖率**：种子池 **217 只 / 净值缓存覆盖 110 只**（与第一轮记录一致 ✅）；落盘 138 个月末
  （6 个月无评分：2014-04~08 池内历史不足、2020-01-31 非交易日——与原脚本同枚举，非新缺陷）。

### B-2 M15 缺陷发现与修复（R3.5 从未跑通的真因）🔴 → ✅

- **现象**：`r35_zoo_redo.py` 在 `df.rename(columns={"S_eng": "pred"})` 处 `KeyError: ['pred'] not in index`。
- **根因**：`_build_ml_panel.py` 的 `keep` 清单**遗漏 V3.7 规则分对照列**。
  `bt_scores_cache` 里有 `S_engine`/`S_v37`，但都没进 `ml_panel.csv` ⇒ 对照臂缺失 ⇒ 脚本必崩。
  **这解释了为何 R3.5 长期停在 🔄——它不是"缓存没建好"，而是建好后也跑不通。**
- **取数依据（实测，非假设）**：全样本 12,383 行 `max|S_engine − S_v37| = 0.0`，`model_version` 全为 `V3.7`
  （因 `engine.finalize` 内 `S_total := S_v37`）⇒ 二者等价，取 `S_v37` 作 V3.7 规则分对照，并加**断言**防未来分叉。
- **变更边界**：只补一列既有数据 + 一条断言；**不改**任何特征/标签/训练协议/判定门。登记为 **M15**。

### B-3 R3.5 结果（客观数字，按 §0.3 门裁决；**不预设方向**）✅

**自身 IC（fwd6 月度，L=5；fwd12 L=11）**

| 配置 | IC (full) | t_naive | **t_HAC** | 显著(|t|≥2) |
|---|---:|---:|---:|:--:|
| **V3.7 规则分（对照）** | +0.1011 | 5.41 | **3.32** | ✅ |
| lasso F2 fwd6_rk | +0.0550 | 2.18 | 1.36 | ❌ |
| enet F2 fwd6_rk | +0.0533 | 2.08 | 1.29 | ❌ |
| svrlin F2 fwd6_z | +0.0640 | 2.59 | 1.63 | ❌ |
| lasso F3 fwd6_rk | +0.0473 | 1.79 | 1.06 | ❌ |
| huber F2 fwd6_z | +0.0584 | 2.30 | 1.43 | ❌ |
| huber F2 **fwd12_z** | **−0.0842** | −3.80 | **−2.00** | ✅(负向) |

**配对差 V3.7 − ML（§0.3 变体门：HAC t>2 且双窗同向）**

| 配置 | full t_HAC | w2019 t_HAC | 过门? |
|---|---:|---:|:--:|
| lasso F2 fwd6_rk | 1.30 | 1.48 | ❌ |
| enet F2 fwd6_rk | 1.29 | 1.53 | ❌ |
| svrlin F2 fwd6_z | 1.10 | 1.55 | ❌ |
| lasso F3 fwd6_rk | 1.43 | 1.72 | ❌ |
| huber F2 fwd6_z | 1.17 | 1.53 | ❌ |
| huber F2 fwd12_z | **3.36** | **2.33** | ✅ |

**按门裁决（只看数字，不事后改门）**
1. **落"无统计区分"一档**：5 个 fwd6 ML 配置自身 IC 全不显著（HAC 1.06~1.63），
   V3.7−ML 配对差也全不过门（1.10~1.72）⇒ **既不能说 ML 反超，也不能说 V3.7 显著更优**。
   → 处置：**维持 V3.7、不引入 ML 腿**（理由是"台账内无法证伪现状"，**不是**"V3.7 被证明更强"）。
2. **F4 前视爆炸半径被实测证实**：fwd12 训练截止 q−6M→q−12M 后 IC **+0.0773 → −0.0842（符号翻转）**，
   配对差 HAC 3.36 显著 ⇒ 该配置历史成绩确由偷看未来 6 个月的标签支撑；爆炸半径限于单一配置（与审计 F4 判断一致）。
3. **历史表述降级被量化**：naive 口径下配对差 2.09~3.11 看似显著，HAC 后 1.10~1.72 全失效
   ⇒ 报告"全部领先或并列第一/双冠"式表述**无统计支撑**，按 F7.2 勘误降级。

**如实披露的局限（削弱本结论强度，一并入库）**
- 半池（110/217）+ 幸存池并集 ⇒ **勘误级证据，非 FULL-PIT 裁决**；
- **w2015 窗与 full 窗塌缩**（冷启动起点 2016-04 晚于 2015-01 门槛）⇒ 本轮实为 full/w2019 双窗；
- **#24 步骤 6（端到端 sim_core 三窗口表）未完成** ⇒ C7 现为 IC 层结论，模型级双门未宣称通过。

### B-4 待办（本轮未完成，如实登记）
- ~~#24 步骤 6：端到端三窗口表~~ → **✅ 2026-09-02 已完成**，详见 §7。
- 阶段 C（S6.1 11 组网格）：经用户裁定**本轮押后**（优先做透 R3.5）；预算按本机实测重估为 **14–18h 墙钟**。

---

# 附：收官交接（结论汇总 / 待办清单 / 改进方向 / S6.1 本地手册）

> 原 `docs/V5_收官交接_结论汇总与待办清单_2026-09-02.md`，2026-09-02 文件整理时并入本执行台账，内容逐字未改。


> 日期：2026-09-02（Asia/Shanghai） · 分支：`arena/01a05d9b-quant-fund-picker`
>
> **口径铁律（不变）**：本文所有数字口径均为 **SURV-ADJ（幸存池 + 复权净值）**，
> 仅供**差额量化与方向探查**，**一律不得**作为任何模型改动的最终采纳/否决依据。
> 最终裁决只能走 **FULL-PIT（D0.6 冻结口径，2006–2026 全基金池、严格 PIT）**，目前仍被 D0.1/D0.3/D0.5 的外部数据依赖阻塞。
>
> **不预设结果**：以下每条结论都对应一次已落盘、可复算的真实数据实验；"无统计区分"与"有效"同为合法结论，不做美化。

---

## 1. 本轮（第三轮沙箱）已完成事项一览

| 编号 | 事项 | 状态 | 关键证据 |
|---|---|---|---|
| E-1/E-2 | 环境重建（venv + 锁版本）| ✅ | pandas 3.0.5 / numpy 2.4.6 / sklearn 1.9.0；`pytest tests/test_research_invariants.py` **28 passed** |
| D0.2 | 复权净值 `cache/navadj_*` 重建 | ✅ | 4,987 只成功 / 0 错；事件日 21,454 覆盖 3,494 只；raw vs adj 年化差 n=4,090 均值 +0.6215%/yr、中位 +0.028%、P95 +3.91%、max +78.78%（与主契约**零偏离**）|
| A-1 | canonical 面板重建 | ✅ | 145 月 / 52,152 行（2014-03-31 → 2026-03-31），与 M11 后权威面板**逐项一致**，耗时 2,316s |
| A-3 | R3.1–R3.4 HAC 复核复现 | ✅ | 全量翻转数、t 值逐行一致（见 §2.1）|
| A-4 | B2.1–B2.3 权威基线复现 | ✅ | R0 +5.08% → R1 +8.59% → R2 +10.73% → R3 +10.84% / MaxDD −18.5%（零偏离）|
| A-5 | S6.2 243 格邻域复现 | ✅ | 基线分位 82%，CAGR 全域 [+5.43%, +12.53%] |
| A-6 | C1/C2 裁决复现 | ✅ | 双门判定与既有裁决完全一致（见 §2.3）|
| A-7 | m12b HAC 余项复现 | ✅ | HP4A-3 OOS 中间门 t_HAC 1.61 翻转仍成立 |
| #24 | R3.5 模型动物园（IC 层）| ✅ **本轮新出结论** | 见 §2.4；产物 `output/v5/r35_zoo_redo/` |
| M15 | `_build_ml_panel.py` 缺 `S_eng` 对照臂 → 修复 | ✅ | 见 §3 |
| — | 三次沙箱重置事故登记与资产抢救 | ✅ | `aa59464` / `291255c` / `3744e3b` 均已推送 |

---

## 2. 实测结论（客观数字，SURV-ADJ 口径）

### 2.1 A 链 HAC 复核：采纳链无一翻黑
- **R3.1**：全因子翻转 4/39，分组翻转 6/35。翻转项：`value fwd3`(−2.16→−1.67)、`alpha_ir fwd6`(2.41→1.30)、`mom4 fwd3`(2.56→1.91)、`eng_value fwd3`(−2.24→−1.72)。
- **R3.2**：`P12_S1_正交_原权−S0` fwd3(−2.56→−1.91)、fwd6(−3.12→−1.95) 翻转；`P12_S2_正交_IC权−S0` fwd6 (−4.05→−2.45) **存活**。
- **R3.3**：`P22_ntuw252` fwd3 IC +0.0504 t_HAC 2.87、fwd6 +0.0505 t_HAC 2.33 **存活**；fwd1 翻转。P2-A 配对差全部不显著。
- **R3.4**：生产 V3.7(S_eng) − V0 IC +0.0209 t_HAC 3.32，**0 处翻转**。
- **结论**：现行采纳链在 HAC 严口径下未出现"由显著变不显著"的采纳级翻黑；**维持现状**。

### 2.2 执行口径分解（B2.x，唯一权威基线，禁再改口径）
R0 +5.08% / MaxDD −22.4% → R1 +8.59% / −18.7% → R2 +10.73% / −18.9% → R3 +10.84% / −18.5%。
延迟敏感性：delay=0 +9.55%、delay=1 +10.84%、delay=2 +10.74% → **T+1 执行不是"甜点参数"，延迟 2 天也不塌**，稳健。

### 2.3 C1 / C2 裁决（双门，均维持现状）
- **C1（去掉 MDD 惩罚）**：H1 OOS 配对 IC t_HAC = **1.11 未过**；H2 ΔCAGR +1.36pp、ΔMaxDD −0.01pp 过。**双门未同时通过 → MDD 惩罚保留**。
- **C2（动量平滑 M2 替 M1）**：H1 t_HAC = **−0.47 未过**；H2 ΔCAGR **−1.45pp**、ΔMaxDD **−4.31pp** 皆负。**否决 M2，维持 M1**。

### 2.4 R3.5 模型动物园（#24，本轮核心新结论）
- 样本：SURV-ADJ 半池 **110/217 只**，面板 **12,383 行 × 138 月末**，全体配置共同冷启动 **2016-04-30**。
- **对照臂 V3.7 规则分**：full n=123，IC **+0.1011**，t_naive 5.41，**t_HAC 3.32**（显著）。w2019 子窗 IC +0.0989，t_HAC 2.32。
- **5 个 fwd6 ML 决赛配置**（lasso/enet/svrlin/lasso-F3/huber）：修正后 IC 0.0473 ~ 0.0640，**t_HAC 1.06 ~ 1.63，全部不显著**。
- **配对差 V3.7 − ML**：t_HAC 1.10 ~ 1.72，**全部未过 2.0**。
- **判定**：三种合法结论中落在 **「无统计区分」**一档 —— 既不能宣称 V3.7 显著优于 ML，也不能宣称 ML 反超。
  **ML 腿不构成候选，维持现状（台账 C7）**。
- **F4 前视爆炸半径实测证实**：`huber F2 fwd12_z` 把标签窗从 q−6M 改到 q−12M 后，
  历史 IC **+0.0773 → 修正后 −0.0842（t_HAC −2.00）**，V3.7 与其配对差 t_HAC **3.36**。
  即：**一处标签窗前视足以把一个"看似最好的模型"翻成显著为负**——这是本轮最有价值的方法论证据。
- **窗口口径披露**：因共同冷启动 2016-04-30 晚于 w2015 阈值 2015-01-31，**full 与 w2015 逐行相同**（已验证 `True`），
  故 R3.5 实际只有 2 个独立窗口而非 3 个，报告中不得宣称"三窗一致"。

---

## 3. 本轮发现并登记的工程缺陷（M13–M15）

| ID | 缺陷 | 数值影响 | 处置 |
|---|---|---|---|
| **M13** | `p1_panel_build.build_date()` 内 `ThreadPoolExecutor(max_workers=2)` 硬编码，`--workers` 入参被**静默忽略** | 无（M11 修复后按 code 稳定排序，结果与线程数无关）；仅影响吞吐 | 本轮不改（改动会破坏 S6.1 与 canonical 的运行条件对称性）→ 待 F7。**S6.1 本地跑时 `--workers 4` 实际无效，见 §5** |
| **M14** | `build_universe()` 读写 `universe.csv` 时跟随 `--panel-dir`，S6.1 各变体目录会**各自重建** | 输入同源应一致，但未校验 | S6.1 跑完必须校验各变体 `universe.csv` 的 sha256 与 canonical 一致；不一致立即立案停跑 |
| **M15** | `_build_ml_panel.py` 丢弃 `S_engine`/`S_v37`，导致 `r35_zoo_redo.py` 取对照臂时 `KeyError: ['pred']` | **阻断 R3.5**（本轮已修）| 已修：取 `S_v37`（缺则 `S_engine`）为 `S_eng`，两列并存时断言 `max\|Δ\|==0` 否则停跑待裁。实测 12,383 行两列 na=0、`max\|Δ\|=0.0`、`model_version` 唯一 `V3.7` |
| **M12** | 不变量测试在产物缺失时**静默 skip**（曾出现 27 passed **+1 skipped** 而无人察觉）| 治理风险 | **✅ 已修（2026-09-02，C8 同批）**：产物缺失即 FAIL 并打印缺失路径（见 §8）|

---

## 4. 仍未完成的项目（交接清单）

| 优先级 | 项目 | 现状 | 完成标准 |
|---|---|---|---|
| **P0** | **FULL-PIT 全池裁决通道（D0.1 / D0.3 / D0.5）** | ⛔ 被外部数据阻塞（需 2006 起全量申赎状态/成立退市/分红拆分的 PIT 快照） | 打通后**重跑全部已裁决项**（C1/C2/R3.x/R3.5），当前所有 SURV-ADJ 结论一律视为"待复核" |
| **P1** | **#24 步骤 6：R3.5 端到端 sim_core 三窗表** | ⬜ 未做。当前 C7 只是 **IC 层**结论，非模型层双门结论 | 用 5 个 fwd6 决赛配置各自出组合净值，与 V3.7 基线在同一执行口径（R3、T+1、含成本）下对比 CAGR/MaxDD，补齐 H2 门。注意窗口只有 2 个独立窗（§2.4）|
| **P1** | **S6.1 种子×maxn 网格（#33，11 组）** | ⬜ 用户裁定**本地跑**（本机 2 核需 14–18h 墙钟）| 见 §5 手册。产出 `output/v5/s61_robustness.csv` + `s61_summary.md`。**自封条款：仅报告不平判，禁据其结果调参** |
| **P2** | **M13 修复（`--workers` 真正生效）** | ⬜ | 待 S6.1 跑完后再改，避免破坏运行条件对称性 |
| **P2** | **M12 修复（测试静默 skip → 显式失败）** | ✅ 已修（2026-09-02，见 §8）| 改 `tests/test_research_invariants.py`，产物缺失时 fail 并打印缺失路径 |
| **P2** | **M14 校验（S6.1 各变体 universe.csv 一致性）** | ⬜ | S6.1 跑完立刻做 sha256 比对 |
| **P3** | **全市场 ECDF vs 批内 rank 参照系子维度** | ⬜ 依赖"逐月全宇宙动量参照快照"（`engine.get_global_ref_universe` 目前只取最新快照，构成隐性前视）| 需以 U4.1 宇宙底座重造逐月快照后才可开跑；**在此之前不得宣称该子维度已消解** |
| **P3** | manifest 与产物指纹回填 | 部分 | `v5_manifest.py` 重跑，把本轮 R3.5 产物指纹写入 `output/v5/manifest.jsonl` |

---

## 5. S6.1 本地执行手册（用户自跑）

```bash
# 0) 环境（与沙箱锁版本一致，否则数值不可比）
python3 -m venv .venv_review
.venv_review/bin/pip install -r requirements-v5-lock.txt requests py_mini_racer
# 预期版本：pandas 3.0.5 / numpy 2.4.6 / scikit-learn 1.9.0

# 1) 前置产物（若 cache/navadj_* 为空）
.venv_review/bin/python build_navadj.py --workers 2
#   验收：adj 成功 4,987 / err 0；事件日 21,454 覆盖 3,494 只

# 2) canonical 基准面板（若 output/p1_panel 为空）
.venv_review/bin/python p1_panel_build.py --start 2013-01 --end 2026-03 --maxn 500 --workers 2
#   验收：145 个 CSV，合计 52,152 行

# 3) S6.1 主跑（断点续跑安全，可随时 Ctrl-C 后重跑同一命令）
nohup .venv_review/bin/python s61_runner.py > output/logs/s61.log 2>&1 &
#   11 组 = 种子{1..5}×maxn{500,1000} + 全取×1，每组 159 月
#   ⚠️ M13：runner 里写的 --workers 4 实际被忽略（内部硬编码 2 线程），别据此估时
#   本机 2 核实测外推：14–18h 墙钟；maxn=1000 与"全取"组显著更重

# 4) 汇总
.venv_review/bin/python s61_summarize.py
#   产出 output/v5/s61_robustness.csv + s61_summary.md

# 5) 跑完立刻做 M14 校验（不可省）
for d in output/s61_panel/*/; do sha256sum "$d/universe.csv"; done
sha256sum output/p1_panel/universe.csv
#   全部一致 → 放行；任一不一致 → 停跑立案，S6.1 结果作废
```

**S6.1 判读纪律**：#33 是自封的"仅报告不平判"项。无论组间 IC 均值/HAC t 分布是紧还是散，
**都不得据此调整任何生产参数**；它只回答"canonical 那一组是不是运气"，
落在 SURV-ADJ 口径下，同样**不能**作为采纳/否决依据。

---

## 6. 有助于优化模型的改进方向（基于本轮实测，非预设）

> 以下每条都注明"证据强度"。**没有一条可以在 FULL-PIT 打通前直接上生产**。

1. **【证据强，方法论层】把"标签窗前视"列为一级红线检查项。**
   实测：`fwd12` 一处标签窗错配就把 IC 从 +0.0773 打到 −0.0842（t_HAC −2.00）。
   建议：在 `_build_ml_panel.py` 与所有打标脚本入口加**断言**——标签起点必须 ≥ 特征快照日 + 执行延迟，
   并把"标签窗 / 特征窗 / 执行延迟"三元组写进每份产物的 manifest 头。这是**零成本、高收益**的护栏。

2. **【证据中，模型层】ML 腿短期不值得继续投入，除非先扩样本。**
   R3.5 的 110 只 / 12,383 行 / 冷启动 2016-04 的样本量下，5 个决赛配置 t_HAC 全在 1.06–1.63，
   **不是"ML 不行"的证据，而是"样本不足以区分"的证据**。
   建议顺序：先打通 FULL-PIT 扩到全池（预计样本量 ×10 量级），**再**重跑 R3.5；
   在此之前继续调 ML 超参属于在噪声里挖矿。

3. **【证据中，因子层】`ntuw252`（252 日水下时间占比的负向）是本轮唯一稳健存活的新因子线索。**
   fwd3 IC +0.0504 t_HAC 2.87、fwd6 +0.0505 t_HAC 2.33，HAC 后仍显著且跨窗一致；fwd1 翻转说明它是**中周期**信号。
   建议：作为独立候选走完预登记 → 冻判门 → FULL-PIT 复核流程，**不要**直接塞进 V3.7 权重。

4. **【证据中，风控层】MDD 惩罚保留是对的，但它买的是"防守"不是"收益"。**
   C1 实测去掉 MDD 惩罚 CAGR +1.36pp 而 MaxDD 几乎不变（−0.01pp）。
   这说明当前 MDD 惩罚在 SURV-ADJ 池里**没有兑现风控价值却在吃收益**。
   但 H1 门未过（t_HAC 1.11），所以维持现状是正确处置。
   建议：把它列为 **FULL-PIT 打通后第一批复核项**——幸存者偏差恰恰会系统性低估回撤，
   全池下 MDD 惩罚的价值很可能被重新证实（或彻底否定），这是个高信息量的实验。

5. **【证据强，工程层】执行口径已经稳了，不要再动。**
   B2.3 显示 delay=0/1/2 的 CAGR 为 +9.55%/+10.84%/+10.74%，非尖峰；
   S6.2 243 格邻域基线分位 82%、CAGR 全域 [+5.43%, +12.53%]。
   参数处在平坦区中上位，**继续调参的期望收益低于过拟合风险**。§2.2 口径已封，禁再改。

6. **【证据弱，待验】平滑类变体（V1/V2/V3/V5）方向一致为正但幅度小。**
   R3.4 中 alpha 平滑 +0.0091(t_HAC 2.57)、mom 平滑 M1 +0.0151(2.79)、全平滑 +0.0176(2.34)，
   而 value 平滑为负 −0.0035(−1.08)。
   建议：**不要做"全平滑"**（它的增益基本来自 mom+alpha 两条腿），把 value 排除在平滑之外；
   但幅度都在 1–2pp IC 量级，需 FULL-PIT 复核才值得动权重。

7. **【治理层】把"静默绿灯"当故障处理。**
   M12（产物缺失 → 测试 skip 而非 fail）+ 三次沙箱重置的联合效应是：
   **本地未推送的 commit 与未入 manifest 的产物等于不存在**。
   建议固化两条：①每批实验结束立即 `commit + push`；②任何结论落地前先跑 `v5_manifest.py` 把产物指纹入库。

---

## 7. 口径声明（收尾）

本文第 2 节全部数字为 **SURV-ADJ**，第 6 节全部建议为**方向性线索**。
**在 D0.6 FULL-PIT 通道打通并完成复核之前，不得据本文任一数字对生产模型做采纳或否决。**


---

## 7. 第四次沙箱重置 + #24 步骤 6 补齐（2026-09-02）

### 7.1 第四次重置事故登记
- **现象**：新回合开工核查（铁律 6：先取证再行动）发现本地 `git log` 回退至 `ec2ee7d`，
  venv、`cache/navadj_*`、`output/*` 全损；`cache/nav_*` 15,444 只与全部 .py/.md 保住。
- **关键差异（相对前三次）**：**零结论损失**。因上一回合 GH 通道恢复后已把 `291255c`/`3744e3b`/`36d7134`
  全部推送，`git ls-remote` 显示远端完好在 `36d7134` ⇒ `git fetch && git reset --hard FETCH_HEAD` 即全量恢复。
- **结论**：铁律 3（"每完成一批立即 commit + push"）在本次事故中**直接兑现价值**，
  代价从"重跑 + 重写结论"降为"仅重跑产物"。此为该纪律的第一次正面实证，登记备查。

### 7.2 产物重建（第四轮）· 逐项零偏离
| 项 | 实测 | 与文档固化值对比 |
|---|---|---|
| venv | pandas 3.0.5 / numpy 2.4.6 / sklearn 1.9.0 | ✅ 一致 |
| D0.2 navadj | 4,987 只 / err 0；事件日 21,454 覆盖 3,494 只；年化差均值 +0.6215%/yr、中位 +0.028%、P95 +3.91%、max +78.78% | ✅ **逐位一致** |
| R3.5 打分缓存 | 3 分片并行，138/144 月（6 个"无评分"月与前轮**同一批**） | ✅ 一致 |
| 缓存确定性 | `--verify 2020-06-30` → 落盘 `c23979c1b9c4c062` vs 重算 `c23979c1b9c4c062` | ✅ 且与**重置前**同一哈希 ⇒ **跨重置确定性成立** |
| ML 面板 | 12,383 行 / 138 月 / 110 只；`S_eng` na=0 | ✅ 一致（M15 修复随 git 恢复，无需重打） |
| R3.5 IC 层 | 7 臂 IC/t 全表（V3.7 +0.1011/3.32；fwd12 −0.0842/−2.00） | ✅ **逐位一致** |
| M1.1 闸门 | `pytest tests/test_research_invariants.py` → **28 passed** | ✅ 常绿 |

### 7.3 #24 步骤 6：端到端 sim_core（本回合 P1 主目标）✅

**预登记（冻结于开跑前，随 `r35_e2e.py` commit `8f8bddd` 入库，跑完未改一字）**
- 分数可比化协议：ML 预测为 z/rank 量纲，直接喂 sim_core 会让 buy=70/sell=45 的**门槛错配冒充模型差异**。
  故统一做**逐月分位映射**到同月 V3.7 分数经验分布：① 严格保序 ⇒ 不改任何臂的选基排序（不损失模型信息）；
  ② 各臂逐月分数分布与 V3.7 完全相同 ⇒ 买卖门/槽位/CPPI/危机层对所有臂**对称**；③ 对 V3.7 臂是**恒等变换**。
  这是唯一允许的分数变换，不得再引入逐臂调参。
- 执行口径 = B2.1 R3 逐字一致；窗口 = full / w2019（w2015 已知塌缩，如实只报双窗）。
- **H2 门**：某臂构成候选 ⟺ **两窗均**满足 ΔCAGR>0 且 ΔMaxDD 劣化≤2pp 且 ΔCalmar≥0。

**实测结果**

| 窗口 | 臂 | CAGR | MaxDD | Calmar | 换手 | 成本 | H2 |
|---|---|---|---|---|---|---|---|
| full | **V3.7 规则分（对照）** | **+7.32%** | **−21.7%** | 0.337 | 58 | 3,848 | — |
| full | lasso F2 fwd6_rk | +3.47% | −24.4% | 0.143 | 94 | 7,297 | ❌ |
| full | enet F2 fwd6_rk | +2.16% | −36.1% | 0.060 | 102 | 7,684 | ❌ |
| full | svrlin F2 fwd6_z | +3.67% | −26.4% | 0.139 | 89 | 6,929 | ❌ |
| full | lasso F3 fwd6_rk | +3.17% | −28.0% | 0.113 | 88 | 6,707 | ❌ |
| full | huber F2 fwd6_z | +3.11% | −24.0% | 0.130 | 93 | 7,611 | ❌ |
| full | huber F2 fwd12_z | +0.69% | −36.7% | 0.019 | 115 | 8,502 | ❌ |
| w2019 | **V3.7 规则分（对照）** | **+13.96%** | **−14.4%** | 0.972 | 41 | 3,465 | — |
| w2019 | lasso F2 fwd6_rk | +4.56% | −24.0% | 0.190 | 54 | 4,704 | ❌ |
| w2019 | enet F2 fwd6_rk | +1.81% | −38.5% | 0.047 | 81 | 6,651 | ❌ |
| w2019 | svrlin F2 fwd6_z | +6.28% | −24.6% | 0.255 | 52 | 4,759 | ❌ |
| w2019 | lasso F3 fwd6_rk | +2.99% | −38.0% | 0.079 | 78 | 6,489 | ❌ |
| w2019 | huber F2 fwd6_z | +6.37% | −26.3% | 0.243 | 57 | 5,258 | ❌ |
| w2019 | huber F2 fwd12_z | +2.27% | −36.8% | 0.062 | 72 | 5,608 | ❌ |

**按门裁决（只看数字，不事后改门）**
1. **6 臂 × 2 窗 × 3 指标 = 36 项比较，无一优于对照臂** ⇒ **H2 门全线未过**。
2. 叠加 H1（IC 配对差 HAC 全未过 2）⇒ **双门皆堵：ML 腿不构成候选，维持 V3.7**（C7 终态）。
3. **H1 与 H2 的"未过"含义不同，禁止混report**：H1 = **区分不出来**（统计功效不足，见 C9）；
   H2 = **方向明确劣于**（差额大、两窗一致、三指标同向）。正确合并表述：
   *在当前 SURV-ADJ 半池口径下，ML 腿在组合层面明确不占优；IC 层的功效不足以支撑更强的因果主张。*
4. **一致的机制线索（登记为观察，非结论）**：全部 ML 臂换手更高（88~115 vs 58）、成本更高（1.8~2.2×）、
   回撤更深。IC 层"无统计区分"叠加组合层"明确劣化"的组合，指向**信号稳定性/换手成本**而非**信号方向**的问题。
   该线索**不足以立案**，登记待 A5.2 全池复核时检验。

**如实披露的局限（削弱本结论强度）**
- 宇宙仅 110 只而 slots=10（池深占比约 9%）⇒ 高换手臂在浅池被成本与冲击放大，**H2 劣化幅度可能被系统性夸大**；
- 本表 V3.7 的 full +7.32% 与 B2.1 R3 的 +10.84% **不可比**（宇宙 110 vs 3,485、起点 2016-04 vs 2014-03），
  **不构成任何基线修正**；B2.1 R3 仍是 SURV-ADJ 唯一权威基线（C5）；
- 双窗而非三窗（w2015 塌缩），原 #24 步骤 6 的"三窗口表"措辞据实降级；
- 全部为 SURV-ADJ 半池证据，**非 FULL-PIT 裁决**；终审在 A5.2。

### 7.4 文件整理（2026-09-02）
| 动作 | 对象 | 理由 |
|---|---|---|
| 合并 | 勘误追加段 P1 + P2 → `勘误追加段_HAC复核_P1P2_2026-09-01.md` | 两文各 24/39 行、同批次同口径，碎片化无收益；**原文逐字未改** |
| 合并 | 产物重建执行计划 + 收官交接 → `V5_执行台账_产物重建与收官交接_2026-09.md`（本文） | 同一轮工单的计划与收尾，分立两文导致待办清单双源 |
| 删除 | `r35_rebuild_zoo_cache.py` | 与 `r35_zoo_cache_shard.py --nshards 1 --shard 0` **逐字节等价**，属冗余；等价性已写入分片版头注 |
| 合并 | C7~C11 全部结论并入 `V5_模型改进候选台账_2026-09.md`（新增索引表） | 遵用户要求：结论单一权威载体 = 候选台账 |
| 新增 | `r35_e2e.py` | #24 步骤 6 执行器；预登记门随脚本先行入库 |

> 未做的整理（如实说明）：`backtest.py` / `backtest_local.py` / `strategy_bt.py` / `p3_backtest.py` / `sim_core.py`
> 五个模拟器看似重复，但 M1.3 已完成三合一对拍（max|Δ|=0）且**各自仍被不同历史脚本引用**；
> 合并属于**改动运行时**，会破坏既有产物的可复现性，**不在文件整理的授权范围内**，登记待 F7 统一处置。

---

## 8. 研究纪律护栏批次：C8 + M12 落地（2026-09-02）

> 本批是**工程护栏**，非实验、非模型改动：**不改变任何既有数字**（不改 fwd/标签/特征/训练样本/评分/执行参数），
> 只把"标签窗前视 + 测试静默降级"两类失败模式从"可能无声发生"改为"必被硬断言/显式 FAIL 拦下"。
> 按候选台账 C8 定义，护栏/排序/口径类不适用 §0.3 统计门。

### 8.1 交付物（全部入库，产物指纹在 commit 内）
| 文件 | 动作 | 内容 |
|---|---|---|
| `research_guard.py` | **新增** | C8 护栏模块：`LabelSpec`（四元组 `{标签窗,特征窗,训练截止规则,执行延迟}`）、`validate_label_contract`（标签起点 ≥ 特征快照日+执行延迟，raise 不可被 -O 关）、`assert_label_start_not_in_future`（delay=0 越过决策日=前视）、`assert_train_cutoff_h_matched`（F4 复发闸）、`resolve_first_tradeable_date`、`write_contract_sidecar` |
| `_build_ml_panel.py` | 修改 | F4 主病灶打标入口：构建 fwd3/6/12 后逐行校验 base=asof(决策日)；契约 `ml_panel`(delay=0) 落盘 sidecar + manifest jsonl；**fwd/标签数值零改动** |
| `p1_panel_build.py` | 修改 | canonical 打标入口：契约 `p1_panel_canonical`(fwd1/3/6=21/63/126 交易日, delay=0) 写入 PANEL_DIR manifest 头 |
| `tests/test_research_invariants.py` | 修改 | **M12**：`160222` 真值锚点由 `@skipif` 静默 skip → 产物缺失即 FAIL 并打印缺失路径；新增 `TestLabelContract`（C8）×10 用例 |
| `docs/V5_模型改进候选台账_2026-09.md` | 修改 | C8 当前判定 → ✅ 已实施 + 实施记录 |
| 本文件 §3/§4 | 修改 | M12 处置/交接状态 → 已修 |

### 8.2 M12 语义确认（取证，非臆断）
- 病灶：`cache/navadj_*` 全损时该套件报 "27 passed + **1 skipped**"（复现于执行台账 A-2），绿灯假象无人察觉。
- 修复后实证：临时移走 `cache/navadj_160222.csv` → `pytest -k 160222` 报 **FAIL**：
  `M12: 产物缺失 → 真值锚点测试 FAIL（禁止静默 skip）。缺失路径: cache/navadj_160222.csv；复现: build_navadj.py --workers 2 ...`
- 锚点文件经真实构建器 `build_navadj.build_one("160222")` 重建后，套件回到全绿。

### 8.3 验收
- `pytest tests/test_research_invariants.py -q` → **38 passed / 0 fail / 0 skip**（原 28 + C8×10）。
- C8 护栏**只增检出、不改数字**：`_build_ml_panel.py` / `p1_panel_build.py` 的 keep 列与 fwd 计算未动；
  标签口径 exec_delay=0（base=决策日当月 bar），与执行层 sim T+1(D0.4) **分离打标、不混报**（铁律 1）。

### 8.4 环境备注
- 沙箱 Linux 无既有 env；按 `requirements-v5-lock.txt`（pandas 3.0.5/numpy 2.4.6/sklearn 1.9.0/scipy 1.17.1/statsmodels 0.15.0/pytest 9.1.1）重建 venv 于沙箱，实测版本逐项吻合。
- `cache/`、`output/` 产物按 `.gitignore` 留在仓库外（本批仅在沙箱重建了 `cache/navadj_160222.csv` 以满足 #16 锚点全绿，不提交）。
