# 量化选基系统 V5 重规范实验总计划与执行记录（2026-09-01 起）

> **总纲**：
> 1. 本文件是"实验协议 + 执行记录"二合一：**每项研究先登记假设与判定规则，完成后只把客观结果写回状态表**。
>    标记约定：✅(按预登记完成) / ❌(完成且按门否决) / 🔶(受阻/噪声，处置已写明) / ⬜(未开始) / 🔄(进行中)。
>    **状态表只追加客观结果数字，不修改任何已写下的判定规则。**
> 2. **不预设任何改动是好的，也不预设任何改动是坏的。** 维持现状与推翻现状同为合法结论；显著为负同样入库。
>    每一行结论必须对应可复现命令与产物路径；口径不达标的数字只可作"修复前后差异"的对照事实，不得作裁决证据。
> 3. 目标数据口径（本计划的存在理由）：**2006-01 → 2026-03、全基金池（含已清盘/合并/转型基金）、严格 Point-in-Time**；
>    执行口径：成交价复权、T+1 成交、全收益基准、统一成本阶梯。
> 4. 预登记纪律：每项实验的假设/步骤/判定门在本文件 §3 登记后**先单独 commit**，运行结果后随 commit（修复审查 M8：时序可外验）。
> 5. 任何新机制/新变体，不经本计划新登记（预登记下一号 **#10** 起）不得进入裁决。
> 6. 幸存池口径完成的工作（Phase 2/3）一律带 **SURV-ADJ** 标签：仅用于口径修复对账与差异量化；
>    最终采纳/否决裁决一律以 Phase 4/5 的 **FULL-PIT**（全基金池）口径为唯一有效证据。
> 7. 本计划起因：`docs/审计报告_实验严谨性审查_2026-09-01.md` 的 F1–F5、M1–M10；每项实验的"修复对象"列明对应编号。

---

## 0. 全局实验协议（全阶段统一）

### 0.1 数据与执行口径定义

| 项 | 目标口径（裁决唯一有效口径） | 过渡口径（产物必须打标，禁止作裁决证据） |
|---|---|---|
| 宇宙 | 2006-01→2026-03 全部曾存续的类型合格基金（偏股/股票/指数股票/灵活/指数-海外），**含已清盘、合并、转型**；同一基金 C/E 份额按 A 或最早份额归并（归并规则在 D0.5 预登记内定义） | 现存基金池（标签 **SURV**，幸存者上界） |
| 成交价/盯市 | **复权净值序列**（由官方日增长率累乘构建 × 首日单位净值定标），移动止损/CPPI/再平衡同序列 | RAW-NAV（仅限差异量化对照） |
| 成交时点 | 信号用 T 日收盘前可得信息计算 → **T+1 个交易日净值成交**；QDII 按其确认规则（QDII 假设 T+2，写入 D0.4 并披露） | T0-CLOSE（仅限历史档案对账） |
| 基准 | 沪深300**全收益**口径（H00300 全收益或经股息率交叉验证的等价序列） | PRICE-IDX（仅限历史档案对账） |
| 成本 | 持有期阶梯（沿用 P3-1 预登记）：买 0.15%；卖 <7d 1.5% / 7-30d 0.75% / 30-180d 0.5% / 180d-2y 0.25% / ≥2y 0；海外 0.5% 平档；T+2 到账占用与滑点不建模（披露） | ZERO-COST（诊断参考） |
| 资格 | 决策日 d 合格 ⇔ d 前净值行数 ≥800 且 d−7 日内有披露；**禁止引用任何 d 之后的信息** | —（无过渡口径，违规即作废重跑） |

### 0.2 严格 PIT 规则（违反任一 = 产物作废）

1. 决策日 d 只允许使用 d 日收盘前实际可得数据：净值截断至 d（含 d）、PE 截断至 d、指数/宏观至 d；
   经理任期/AUM/基金类型**只允许来自历史快照**，快照缺失则该因子禁用并打标（延续引擎 bt 语义）。
2. **宇宙成员资格必须 d 时点可知**：类型/名称/份额归并只能来自 ≤d 的名录快照；早于快照起点时用
   最近一次早于 d 的快照 + 成立日推定，推断所占比例逐月披露（D0.5/D0.6 量化不确定性的预登记规则）。
3. **禁止一切后向资格过滤**：不得用"月末净值是否存在""之后 N 日是否仍有披露""是否存续至今"等未来信息
   决定 d 时点样本资格（修复对象：`_build_ml_panel.py` 的 `nav_last ≥ date+60d`、`backtest.build_pool` 的
   末净值条件、`p1_panel_build.uni.last` 衍生条件的每一次使用都需重新审读是否符合本条）。
4. 前瞻标签 fwd1/3/6/12 仅用于评估；训练样本只允许使用**在决策日已完整成熟**的标签
   （h 个月目标 ⇒ 训练截止 ≤ q−hM；修复 `_model_zoo.py:125` 对 fwd12 硬编码 6M 的错误）。
5. 缺失值填充只允许逐日截面或扩展窗历史统计量；**禁止全样本统计量**（修复 `impute_features` 的 `gmed` 全期中位数兜底）。
6. 截面参照系（动量 ECDF-rank 等）逐日构建、批间一致；批内 rank vs 全市场 ECDF 的差异入 S6.1。

### 0.3 统计裁决门（全阶段统一；替代 V4 §0.3 口径）

| 层级 | 门（预登记、双向） | 规定 |
|---|---|---|
| 单因子 | 月度 Spearman IC：\|IC\|>0.05 且 **HAC t > 2** | HAC = Newey-West：fwd6 月度 L=5、fwd3 L=3、fwd1 L=1、季度 fwd6 L=2；朴素 t 并列报告仅供对照 |
| 因子增量 | 对既有因子正交残差后 IC，HAC t>2 | 防同源信号重复计分 |
| 变体/权重 | 对基线的**配对差序列** HAC t>2 且全期 + 近3年双窗同向 | **禁止以两条 IC 序列各自 t 值大小作比较裁决**（修复审查 F2 同族错误，波及 V3.7.1 采纳记录） |
| 模型级 | 端到端 Calmar 与 MaxDD **双改善**且全期+近3年双窗成立；涉配对比较 Holm 显著；报告 **DSR 相对提升** | DSR 的 N = 截至当次试验的台账计数（随试验递增并写进产物） |
| 多重检验 | 全部变体（含失败）入 PBO 台账；每阶段末重算 CSCV | CSCV 选择规则（mean 或 Calmar）须预登记，不得事后切换；串行/并行分区均报告 |
| 窗口宣告 | 每条结果附 n、起止、缺测规则 | 训练冷启动期从对比窗剔除或全配置统一对齐（修复 M2） |

**翻案规则**：历史裁决仅当 HAC 口径下翻转且双窗同向时才改判；历史报告原文保留，翻案以追加段记录并互链。

### 0.4 产物与可复现纪律

1. 面板/曲线/台账产物带**内容哈希 manifest**（输入哈希 + 代码 commit + 参数签名），阶段产物归档不可变副本（修复审查 M9）。
2. 预登记 commit 先行 → 运行 → 结果 commit；报告只写客观数字与按门裁决；口径标签写入文件名与报告头。
3. 复现命令必须指向仓库内固定脚本；参数全部 CLI/配置化；禁止"会话内改参不落地"。
4. 每项实验结束必须更新 §1 状态表 + §3 结果段 + §4 执行日志（只追加，不修改）。

---

## 1. 状态总览（完成一项标记一项；结论栏只写客观数字）

### Phase 0 — 数据基建（一切 FULL-PIT 裁决的前置）
| 编号 | 实验项 | 状态 | 结论（客观结果） |
|---|---|---|---|
| D0.1 | 全基金池获取：已清盘/合并/转型基金净值（2006-01→2026-03） | ⬜ | |
| D0.2 | 复权口径统一与分红事件对账（修复 F1） | ⬜ | |
| D0.3 | 基准全收益序列获取与交叉验证（修复 M1） | ⬜ | |
| D0.4 | T+1 成交执行层（含 QDII 确认与停复牌披露滞留规则）（修复 F5） | ⬜ | |
| D0.5 | 基金历史名录/类型快照 = PiT 宇宙底座 | ⬜ | |
| D0.6 | 数据质量门与处置表（缺口/异常/停牌滞留/费率缺失，预登记处置） | ⬜ | |

### Phase 1 — 度量层与研究不变量（不需新数据，可立即开工）
| 编号 | 实验项 | 状态 | 结论 |
|---|---|---|---|
| M1.1 | 研究不变量测试 `test_research_invariants.py`（修复 M10） | ⬜ | |
| M1.2 | HAC 统计包 + 历史台账 t 值全量重算（勘误表）（修复 F2） | ⬜ | |
| M1.3 | 执行模拟器并轨为单一实现并对拍（修复 M5/M6） | ⬜ | |
| M1.4 | 产物哈希 manifest 与预登记 commit 纪律落地（修复 M9/M8） | ⬜ | |

### Phase 2 — SURV-ADJ 过渡口径基线重立（幸存池 × 修复执行口径；不作最终裁决）
| 编号 | 实验项 | 状态 | 结论 |
|---|---|---|---|
| B2.1 | V3.7/V3.8 基线复权重跑（复权 + T+1 + 全收益基准 + 阶梯成本） | ⬜ | |
| B2.2 | 成本模型敏感性网格（预登记网格，仅报告不平判） | ⬜ | |
| B2.3 | 执行时滞敏感性 T0 / T+1 / T+2（仅报告不平判） | ⬜ | |

### Phase 3 — 历史裁决统计口径复核（HAC 重判，SURV-ADJ 先行）
| 编号 | 实验项 | 状态 | 结论 |
|---|---|---|---|
| R3.1 | P1 因子体检 HAC 重判（全因子 × fwd1/3/6） | ⬜ | |
| R3.2 | P1-2 / P1-3 / P1-4 HAC 复核（正交化 / 动量窗 / 惩罚项） | ⬜ | |
| R3.3 | P2-A / P2-2 HAC 复核（价值体制门控 / 低波因子族） | ⬜ | |
| R3.4 | V3.6/V3.7 平滑化采纳证据链复核（t=3.42/3.46 与 F_value 软化否决） | ⬜ | |
| R3.5 | 模型动物园修复重跑 + 报告勘误（修复 F4/M2/M7） | ⬜ | |

### Phase 4 — FULL-PIT 全基金池重建（依赖 D0.1/D0.5；解锁前冻结，不许用 SURV 替代）
| 编号 | 实验项 | 状态 | 结论 |
|---|---|---|---|
| U4.1 | `pit_universe.py` 接入产线：2006-01→2026-03 月度 PiT 宇宙快照（含死亡基金） | ⬜ | |
| U4.2 | FULL-PIT 全池评分面板重建（2006-01→2026-03 月度；死亡基金留存至清盘日） | ⬜ | |
| U4.3 | 幸存者偏差定量计量（SURV-ADJ vs FULL-PIT 同协议差值，替换三情景估计） | ⬜ | |
| U4.4 | 稀疏截面期（2006-2012）处理规则预登记与执行 | ⬜ | |

### Phase 5 — FULL-PIT 裁决重跑（一切"采纳/维持"的唯一有效证据）
| 编号 | 实验项 | 状态 | 结论 |
|---|---|---|---|
| A5.1 | 因子体检重跑（全因子，HAC，FULL-PIT） | ⬜ | |
| A5.2 | 模型动物园 2.0（修复后协议，2006-2026 全池严格 walk-forward） | ⬜ | |
| A5.3 | 执行层矩阵重跑（现台账 15 变体同协议仅作重验；任何新变体需新登记） | ⬜ | |
| A5.4 | DSR / CSCV-PBO / Holm 全量再审；翻案如实改判 | ⬜ | |

### Phase 6 — 稳健性与前瞻证据（不裁决，只积累）
| 编号 | 实验项 | 状态 | 结论 |
|---|---|---|---|
| S6.1 | 截面参照系与抽样稳健性（批内 rank vs 全市场 ECDF；种子 ×5；maxn 500/1000/全取） | ⬜ | |
| S6.2 | 规则参数族的邻域稳健性（仅预登记网格的"平坦性"证据，不作采纳） | ⬜ | |
| S6.3 | 前瞻纸面验证（paper tracking）：预登记 12 个月观察期OOS，完全不动模型 | ⬜ | |

### Phase 7 — 终裁与勘误
| 编号 | 实验项 | 状态 | 结论 |
|---|---|---|---|
| F7.1 | V5 终版裁决报告（每条结论附证据链 + 统计口径 + 适用边界） | ⬜ | |
| F7.2 | README 与历史报告勘误（措辞降级清单；原文不删，勘误并列） | ⬜ | |
| F7.3 | 对外数字凡例（每个对外数字 → 口径标签 + 有效窗口 + 复现命令） | ⬜ | |

---

## 2. 阶段依赖图与开工顺序

```
D0.1(全池净值) ─┐
D0.5(历史名录) ─┼─► U4.1 ─► U4.2 ─► U4.3/U4.4 ─► A5.*(唯一裁决口径) ─► F7.*
D0.6(质量门)  ──┘                       ▲
D0.2/D0.3/D0.4(执行口径修复) ─► M1.3 ─► B2.*(SURV-ADJ 对账) ─► R3.*
M1.1/M1.2/M1.4 先行（不依赖任何数据项）
```
**硬性依赖**：A 阶段不得用 SURV-ADJ 产物替代；D0.1 未达标时，A 阶段整体保持 ⬜ 冻结。
**可立即开工**（本仓库离线即可做）：M1.1 / M1.2 / M1.3 / M1.4 / D0.2 / D0.3 / D0.4 / B2.* / R3.*。

---

## 3. 各实验详细协议（先登记，后执行；执行后只在"结果"处追加客观数字）

### 预登记 #10（D0.1）全基金池净值获取与验收

- **修复对象**：F3（幸存者偏差未闭环）的第一前置。
- **目标**：拿到 2006-01→2026-03 间**曾存续**的全部类型合格基金（含已清盘/合并/转型）的日频净值与官方日收益序列。
- **假设**（双向）：H0=本地现存池与全基金池在任意研究统计量上无实质差异；H1=存在差异。本项只建数据，不作裁决。
- **步骤**：
  1. 候选源按优先级尝试并逐一记录可达性（不得口头放弃，需留日志）：
     a. Tushare `fund_basic`(status 全量) + `fund_nav`（2000 积分）经用户本机 `t0_fetch.py` 抓取，导出 `cache/tushare/`；
     b. 若 a 受阻：东财/新浪历史归档接口对已清盘基金的覆盖探测（抽样 50 只已知清盘基金逐一测试）；
     c. 若 a/b 均受阻：用外部统计锚（清盘基金名录可从证监会/基金业协会公告半结构化获取）圈定死亡名单，
        净值序列缺失的部分登记为"已知不可修复缺失率"，进入 D0.6 处置表。
  2. 死亡名单构建：类型合格且终止日 ∈ [2006-01, 2026-03] 的基金全集，落盘 `data/delisted_funds.csv`（code, name, type, status, inception, end_date, source）。
  3. 净值序列抓取：逐基金断点续跑、逐基金原子写；字段 schema 与现有 `cache/nav_*.csv` 完全一致（date, nav, ret）。
  4. **验收规则（全部预登记，任一不达则该子集打"部分覆盖"标并量化缺口）**：
     - 覆盖率：死亡名单中有净值序列的比例 ≥ 90%（分年度披露覆盖密度表）；
     - 抽样对账：随机 30 只（seed=20260901）与源站逐一比对首末净值，误差容忍 0；
     - 留存规则：死亡基金的净值序列**保留至清盘日前最后一个披露日**，不做任何事后续命。
- **产物**：`data/delisted_funds.csv`、`cache/tushare/`（或替代源）、`output/v5/d01_coverage.md`（覆盖密度表 + 缺口清单）。
- **复现**：`python t0_fetch.py --full-history`（用户本机）；沙箱内 `@echo 离线验收脚本`（待写 `d01_validate.py`）。
- **受阻处置**：三源全阻 → 状态 🔶，U/A 阶段整体冻结，SURV-ADJ 口径成为唯一可用口径并**永久带标**，F7.3 凡例如实写明。

### 预登记 #11（D0.2）复权口径统一与分红事件对账（修复 F1）

- **背景**：审计实测 357 只样本中有 820 天 `nav.pct_change()` 与官方 `ret` 背离 >1pp（分红/拆分），最大个案单日 −34.5% vs +2.6%；现全部执行层模拟器用原始 `nav` 成交。
- **假设**：H0=两种口径下策略指标差异在数值噪声内；H1=存在实质差异。本项先统一口径，再量化差异（差异表进 B2.1）。
- **步骤**：
  1. 构建复权序列规范：对每份 `nav_*.csv`，`adj = (1 + ret).cumprod()`，首日定标 = 首日 `nav`；落盘并行文件 `cache/navadj_<code>.csv`（date, adj_nav），不覆盖原文件。
  2. 对账抽检：按分红事件日志（背离 >0.5pp 的日期）分层抽样 100 个事件（seed=20260901），人工/半自动核对分红公告或份额折算公告；
     建立"事件类型"分类账（分红 / 拆分 / 数据错误），数据错误比例 >1% 时立项清洗，≤1% 时列入 D0.6 处置表。
  3. 一致性断言进测试库（与预登记 #16 联动）：adj 序列单调口径、adj 与 nav 首日一致、adj 多月收益与官方复权口径一致。
  4. 切换点：`backtest_local.load_navs` / `p3_backtest.NavLib` / `strategy_bt.load_navs` / `strategy_experiment.py` 统一改读 `navadj_*`（缺文件时构建，不静默回退原始 nav）。
- **完成判据**：四模拟器全部切换到复权序列；对账报告落盘；差异量化行写入 B2.1 表格前留待其统一裁决。
- **产物**：`cache/navadj_*.csv`、`output/v5/d02_dividend_audit.md`、测试用例若干。
- **复现**：`python build_navadj.py`（待写）→ `pytest -q test_research_invariants.py -k adj`。

### 预登记 #12（D0.3）基准全收益序列

- **背景**：全部回测基准为沪深300**价格指数**（M1）；真实可比的应是含股息再投资的全收益。
- **假设**：H0=基准口径选择不改变任何相对裁决；H1=改变。本项只建序列与对账，裁决影响在 B2.1/A5.3 呈现。
- **步骤**：
  1. 获取沪深300全收益（H00300）2006-01→2026-03 日频收盘；源：中证指数官网导出 / akshare 对应接口 / Tushare（若 D0.1 通道可用）。
  2. 交叉验证：全收益 vs 价格指数的差序列应与已知股息率区间一致（年度差值 vs 沪深300历史股息率分布，越界>50%偏差年份逐一核查并记录原因）。
  3. 落盘 `cache/idx_tr_sh000300.csv`；模拟器基准参数化 `bench_src = tr | price`，所有报告双列展示。
- **完成判据**：序列落盘 + 交叉验证表落盘；任何新报告禁止只引用 PRICE-IDX 列。
- **产物**：`cache/idx_tr_sh000300.csv`、`output/v5/d03_bench_xcheck.md`。

### 预登记 #13（D0.4）T+1 成交执行层（修复 F5）

- **假设**：H0=T+1 与 T0-CLOSE 下各策略指标无实质差异；H1=有。差异量化进 B2.3；本项只建机制。
- **步骤**：
  1. 在并轨后的模拟器（预登记 #18）中实现：信号批于 T 日收盘后形成 → 挂起到 T+1 交易日 → 以 T+1 净值成交；
     卖出同理（T 日触发信号 → T+1 卖出）；逐日触发的移动止损/CPPI 在 T 日盘后判定、T+1 生效。
  2. QDII：按其确认规则假设 T+2 确认（固定假设，写入报告头；不做逐基金真实确认日查询，列入 D0.6 局限）。
  3. 停牌/披露滞留：基金最后一个可得净值日距交易日 >7 日 → 视为不可交易（不可买不可卖），持仓方持续以最后复权净值盯市直至恢复披露或清盘日（死亡基金按清盘规则收尾，见 U4.2 预登记）。
  4. 首日不成交约束：T0-CLOSE 历史档案模式保留为参数 `--exec-delay 0`，仅用于对账。
- **完成判据**：同面板同参数下 T+1 与 T0-CLOSE 各出一本账，差异表落盘（数值供 B2.3 裁决呈现）。
- **产物**：模拟器 `--exec-delay` 参数、`output/v5/d04_exec_delay_diff.csv`。

### 预登记 #14（D0.5）历史名录/类型快照（PiT 宇宙底座）

- **修复对象**："类型用今日名录"的时点错配（V4 计划 §0.2(6) 已披露局限）的正式修复。
- **假设**：H0=类型时点错配对宇宙构成与研究统计量无实质影响；H1=有影响（量化进 U4.2 披露段）。
- **步骤**：
  1. 来源分级：a. Tushare `fund_basic` 的历史字段；b. 无法覆盖历史时：用成立日 + 首发类型静态推定 + 变更公告日修正（人工批次上限 200 只，超出部分打"推定型"标）；
  2. 产出月度快照 `data/pit_universe/YYYY-MM-DD.csv`（schema 满足 `pit_universe.py` 既有约定：code/name/fund_type/status/inception_date/known_at；`known_at` 不得晚于快照日）；
  3. C/E 归并规则预登记：同 stem 多份额→保留成立最早份额为代表；跨份额收益差异不入裁决（A/C 费率优化留 P3-2 重置项，不在本计划）。
- **完成判据**：2006-01→2026-03 每月一份快照；`grep -rn "import pit_universe"` 出现真实调用方（修复 M3 死代码）。
- **产物**：`data/pit_universe/*.csv`、归并规则段、`output/v5/d05_universe_audit.md`。

### 预登记 #15（D0.6）数据质量门与处置表

- **原则**：一切数据缺陷**先登记处置规则，后看数值**；禁止"看到问题再决定怎么修"。
- **步骤**：对净值/指数/PE/名录四类数据分别登记：缺口定义与填补规则（默认不填补，标记缺失）、异常跳变检测（单日 |变动|>20% 且与官方 ret 背离 → 隔离待审）、停牌滞留、费率缺失（申购费未知时按 D0.x 费率兜底表并打标）、PE 缺失段处理（估值盲区既有语义延续）。
- **完成判据**：四类数据各一份处置表落盘；新数据进库前必须过处置表。
- **产物**：`docs/V5_数据质量门与处置表.md`。

---

### 预登记 #16（M1.1）研究不变量测试（修复 M10）

- **定位**：不是实验，是防回归闸门；"研究结论必须先过不变量测试再谈数字"。
- **步骤**（全部构造合成数据，含已知答案）：
  1. **PiT 不变量**：对同一面板，把 as_of 之后的净值/PE/指数数据随机打乱，重新评分，要求 S 逐位不变（断言级 ==）；此用例回归检测 F4/F5 类前视。
  2. **标签对齐**：构造单调价格序列，断言 fwd6(d) 只引用 >d 的价格；训练截止规则断言：fwd12 样本必须 ≤ q−12M。
  3. **RBSA 约束**：合成共线面板，断言权重非负且和=1；断网降级路径不衰变异常。
  4. **统计实现**：DSR/CSCV/Holm 对已知构造序列的闭式答案断言（±1e-6）；Newey-West 与 statsmodels 参考实现对拍（若 statsmodels 可用，否则对拍手写参考）。
  5. **复权一致性**：成交价序列与信号序列同源（同一 adj 构建器）断言。
- **完成判据**：`pytest -q` 全绿且用例数 ≥15；后续每个 Phase 产物生成脚本以一个不变量测试失败为先决熔断。

### 预登记 #17（M1.2）HAC 统计包与历史 t 值全量重算（修复 F2）

- **假设**（双向，逐条独立）：对台账中每个历史 t 判定，H0=HAC 口径下判定不变；H1=判定翻转。**不预设翻转方向。**
- **步骤**：
  1. 实现 `stats_hac.py`：Newey-West t（Bartlett 核，L 按 §0.3 表自动取）；与参考实现互验（#16.4）。
  2. 全量重算台账：P1 全因子×3horizon；P1-2/P1-3/P1-4 全部配对差；P2-A/P2-2；wf_backtest/factor_study V3.6-3.7 采纳链；rbsa_ew；HP4A-3 配对段；动物园 IC 列。
  3. 生成**勘误表**：每行列（报告、原 t、原判定、HAC t、判定是否翻转），翻转项转入 §0.3 翻案规则处理。
- **完成判据**：勘误表落盘且在未来报告中替代旧 t；README 引用的历史 t 值全部加"（朴素）"或替换为 HAC。
- **产物**：`stats_hac.py`、`output/v5/m12_hac_errata.csv`。
- **复现**：`python m12_rerun_hac.py`（待写）。

### 预登记 #18（M1.3）执行模拟器并轨（修复 M5/M6）

- **背景**：现有四套模拟器（`backtest_local.simulate`、`strategy_bt.simulate`、`p3_backtest.simulate`、`strategy_experiment`）语义不一致（跌出面板卖 vs 持有；成本平档 vs 阶梯；净值原始 vs 复权；同日 vs T+1 缺失）。
- **假设**：H0=四套实现在统一参数下产出一致（允许浮点噪声）；H1=存在语义分叉（全部列出并统一语义）。
- **步骤**：
  1. 抽取单一模拟器核心 `sim_core.py`：参数化 `exec_delay ∈ {0,1}`、`cost_model ∈ {zero, flat, ladder}`、`universe_source ∈ {json, panel}`、`absent_position_policy ∈ {hold, sell}`（显式参数，不再有静默差异）。
  2. **对拍协议**：固定 3 个合成人造面板 + 1 个真实短窗口（2019-01→2020-12），新旧实现在同一参数签名下逐日净值对拍，最大差 ≤1e-8 才算并轨成功；分叉点逐笔列出（code/日期/原因）。
  3. 旧脚本改为薄封装调用 `sim_core`；保留历史产物可复现路径。
- **完成判据**：对拍表全绿；四套脚本复跑同一命令产出同一组数字（±1e-8）。
- **产物**：`sim_core.py`、`output/v5/m13_parity.md`。

### 预登记 #19（M1.4）产物哈希 manifest 与 commit 纪律（修复 M8/M9）

- **步骤**：1. manifest 生成器（输入文件 sha256 前 16 位 + 代码 commit + 参数 json + 运行时刻）落盘到每个产物目录；
  2. `.git/hooks` 或 CI 说明文档化"预登记先行 commit"规则；3. 阶段产物不可变副本（tar 归档到本地/外部制品存储，仓库只放 manifest）。
- **完成判据**：本计划之后每一项实验的产物都带 manifest；后续审查者可凭 manifest 重建 byte-identical 面板。

---

### 预登记 #20（B2.1）SURV-ADJ 基线重立（复权 + T+1 + 全收益基准 + 阶梯成本）

- **定位**：幸存池上的**口径修复对账基线**（非最终裁决）。标签 SURV-ADJ。
- **假设**：H0=口径修复不改变 V3.7/V3.8 相对结论的符号与排序；H1=改变。双向均入库。
- **步骤**：
  1. 面板沿用 P1-0 重建产物（幸存池，2014-03→2026-03；2006-2013 段因幸存池覆盖不足在本阶段不宣称）；
  2. 模拟器 = 并轨后的 `sim_core.py`，参数：exec_delay=1、cost=ladder、bench=全收益、价格=navadj；
  3. 重跑基线 v38_cost 同规则；同步出 T0-CLOSE/RAW-NAV/PRICE-IDX 对照账（仅限对账展示）；
  4. 输出"口径修复差异分解表"：旧口径 → +复权 → +T+1 → +全收益基准 → +阶梯成本，逐项 ΔCAGR/ΔMaxDD/ΔCalmar。
- **完成判据**：差异分解表落盘；新基线数字成为 SURV-ADJ 口径下唯一基线（旧 6.16% 口径封存为历史档案）。
- **产物**：`output/v5/b21_baseline_summary.md`、`output/v5/b21_decomposition.csv`。

### 预登记 #21（B2.2）成本敏感性网格（仅报告）

- **假设**：H0=基线结论对成本档选择不敏感；H1=敏感。**本项不做采纳/否决裁决，仅量化弹性。**
- **网格（预登记，事后不得增删）**：买入 ∈ {0, 0.15%, 1.5%}；赎回阶梯整体 ∈ {0.5×, 1×, 2×}；滑点 ∈ {0, 10bp, 20bp}；共 3×3×3=27 格。
- **步骤**：逐格跑基线同参数回测；输出 27 行指标；报告弹性最大的单维与完整网格。
- **产物**：`output/v5/b22_cost_grid.csv`。

### 预登记 #22（B2.3）执行时滞敏感性（仅报告）

- **假设**：H0=exec_delay ∈ {0,1,2} 不改变基线排名特征；H1=改变。
- **步骤**：同一基线分别 exec_delay = 0 / 1 / 2 重跑；逐笔台账对齐分析：统计"信号日→成交日"均价差的分布（分位表），识别受影响最大的规则（移动止损/CPPI/右侧解锁）。
- **产物**：`output/v5/b23_delay_diff.csv`、逐笔差异分布表。

---

### 预登记 #23（R3.1–R3.4）历史裁决的 HAC 复核

（共用协议：输入 = 各历史研究的原始产物；统计 = `stats_hac.py`；判定规则 = §0.3；**只重算统计，不改模型**）

- **R3.1 P1 因子体检 HAC 重判**：
  1. 对 P1-1 全部因子 × fwd1/3/6 算 HAC t（L 按 §0.3），并列朴素 t；
  2. 输出翻转清单（含 mom7 4.43、trend_ma20 2.18、alpha_ir 2.63 等边界项）；
  3. 按 §0.3 翻案规则逐条处置并入库。
- **R3.2 P1-2/P1-3/P1-4 复核**：全部配对差序列重算 HAC t；正交化否决（t −3.05）、动量窗维持、MDD 惩罚保留（t −2.16）逐条复核；翻转则启动改判追加段。
- **R3.3 P2-A/P2-2 复核**：V1/V2/V3 配对差与 tuw252（3.97）重算；tuw252 的 H-P2E 待复测项改到 A5.1 的 FULL-PIT 面板上执行。
- **R3.4 V3.6/V3.7 平滑化采纳证据链复核**：
  1. factor_study 变体表重跑 HAC（原证据 t=3.42/3.46，季度×fwd6 → L=2）；
  2. **原裁决用的比较方式作废**（两条 IC 各自 t 值比较），统一改为配对差 HAC t 重判每次采纳（V1/V2 采纳、S4 否决逐一复核）；
  3. 输出"V3.7 采纳链复核表"：每次采纳在新口径下维持/翻转；**模型本身暂不改**（改动裁决权在 A 阶段）。
- **完成判据**：四张复核表落盘；任何翻转按翻案规则入追加段，历史报告不改原文。
- **产物**：`output/v5/r31_*.csv`、`r32_*.csv`、`r33_*.csv`、`r34_*.csv`。

### 预登记 #24（R3.5）模型动物园修复重跑 + 勘误（SURV-ADJ）

- **修复对象**：F4（fwd12 前视 + 全样本中位数兜底）、M2（冷启动不对称 + "全部领先"表述错误）、M7（"235季"术语）。
- **假设**（双向）：H0=修复后 V3.7 仍不低于最优 ML（含配对差显著性）；H1=ML 反超。**不预设方向；维持与翻转同为结论。**
- **步骤**：
  1. 面板重建（幸存池口径，标签 SURV-ADJ）：删除 `_build_ml_panel.py` 的 `nav_last ≥ date+60d` 后向过滤；缺失值改扩展窗中位数；
  2. 训练截止规则改 h 匹配（fwd3→q−3M、fwd6→q−6M、fwd12→q−12M），逐目标断言进 #16 测试；
  3. **冷启动对齐**：所有配置（含 V3.7 纯分）统一从"全体配置均有预测的首月"起评；full 窗口重新定义为该起点，原始 2006 起点窗口保留为档案对照并显著标注不对称期；
  4. 对比统计缺口补齐：V3.7 vs 每个决赛配置的**配对差 HAC t**；V3.7 vs 最优 ML 的配对差序列与置信区间；
  5. 报告勘误："全部领先或并列第一"按实测改写（若 w2015 仍为 ML 高，改写为分窗口客观描述）；"235季"改"235 个月末"；
  6. 端到端回测层走并轨 `sim_core`（复权+T+1+全收益基准+阶梯成本），重出三窗口表。
- **完成判据**：勘误后的 `output/model_zoo_report.md` 追加修订段（原文保留）；动物园结论以配对差 HAC t 表述。
- **产物**：`output/v5/r35_zoo_redo/`（全套）、报告修订段。

---

### 预登记 #25（U4.1）PiT 宇宙快照接入产线（依赖 #10/#14）

- **步骤**：
  1. `pit_universe.py` 从死代码变为产线组件：`PITUniverseStore` 供给 `p1_panel_build`（U4.2）与回测的宇宙层；
  2. 逐月生成 2006-01→2026-03 快照并算 manifest 哈希；每娃快照含死亡基金（状态字段如实）；
  3. 宇宙验证打印：逐月合格数、推定型占比、死亡在池数，供 U4.4 稀疏期判定输入。
- **完成判据**：任一历史月份都能复现当月宇宙（内容哈希可验）；死亡基金在清盘前最后一个披露月正常计入宇宙。

### 预登记 #26（U4.2）FULL-PIT 全池评分面板重建

- **协议**：沿用 P1-0 规则并升级：决策日=当月最后交易日；资格 = 800 行 + d−7 披露 + **PiT 宇宙成员**（来自 #25，无任何后向过滤）；
  抽样：截面 >1000 时按种子抽样 1000（seed=YYYYMMDD；maxn 敏感性入 #33）；评分 = `score_fund(bt=True)` 同式；
  前瞻标签 fwd1/3/6/12 全量落盘；死亡基金按真实披露末日留存。
- **关键差异（相对 P1-0）**：死亡基金在池至清盘；类型来自历史快照；无 maxn=500 限制（改 1000 并做敏感性）；全程走 M1.4 manifest。
- **完成判据**：2006-01→2026-03 全月落盘 + manifest；稀疏月清单交 #28 处置。
- **产物**：`output/v5_panel/<date>.csv` + `manifest.jsonl`。

### 预登记 #27（U4.3）幸存者偏差定量计量（替换三情景估计）

- **假设**：H0=幸存池口径数字与全池口径数字的差异在披露阈值内（预登记阈值：全期 IC 差 <0.01、基线 CAGR 差 <1pp）；H1=超出阈值。
- **步骤**：同一评分代码、同一决策月集，分别对 SURV-ADJ 池与 FULL-PIT 池产出：IC 全套（HAC）、基线回测全套（复权+T+1+全收益+阶梯成本）；
  差值分解 = 宇宙差异纯效应；与 V3.7.2 的三情景估计（+9.0%~10.3%）对账并评注。
- **完成判据**：差异量化表落盘；**此后 README 一切绝对收益数字必须按实测偏差标注下修区间或放弃绝对口径**。

### 预登记 #28（U4.4）稀疏截面期（2006-2012）处理规则

- **预登记规则**（执行前写死）：月合格截面 <30 → 该月不参与 IC 汇总但保留面板行；30≤n<100 → 参与但打"薄截面"标；
  权重方案固定为不加权（等权月份）；**禁止**事后改这些阈值；报告必须附 n 分布图。
- **完成判据**：规则进 `p1_panel_build` 类构建器参数；A5 全部结果附带 n 分布。

---

### 预登记 #29（A5.1）FULL-PIT 因子体检重跑

- **范围**：P1-1 全套（13 因子×3 horizon×5 子群）+ P2-2 低波族（含 H-P2E 的 tuw252，在本面板复测，门 = §0.3）+ P2-A 体制条件化，
  **全部 HAC 口径**。
- **假设**：逐因子双向（有意义/无意义均入库）；价值腿的体制依赖性作为预设研究问题登记，不预设应保留还是剔除。
- **完成判据**：全套表格 + 翻转清单；任何"因子入/出库"动议在 A5.4 汇总裁决前不生效。

### 预登记 #30（A5.2）模型动物园 2.0（FULL-PIT 修复后协议）

- **范围**：同构 66 配置（模型族/特征集/目标不变，保证与历史可比）+ V3.7 对照；修复点全部继承 R3.5；
  判定升级：**V3.7 vs 各 ML 一律报配对差 HAC t；端到端层执行口径 = 并轨 sim_core（复权+T+1+全收益+阶梯成本）**。
- **不预设结果**：V3.7 继续最优、ML 反超、两者无统计区分，三者均为合法结论；按 §0.3 门如实裁决。

### 预登记 #31（A5.3）执行层矩阵重跑（FULL-PIT）

- **范围**：现台账 15 变体（base_zero 换标签为诊断、v38_cost 基线、vol_target、no_overlay、conviction、cap25/30/35、p90、sell40、xm_fix3、xm_dyn、roll_hwm、cap_hyst、spit）同协议重跑；
  **新变体一律不许夹带**（需要则开 #37+）。
- **判定**：§0.3 模型级门；与历史裁决逐一对账，翻转走翻案规则。

### 预登记 #32（A5.4）多重检验全量再审

- **范围**：A5.1–A5.3 全部产物 + 台账 N 更新（含历史所有失败变体计数）；DSR/CSCV（mean 与 Calmar 双选择规则均报）/Holm/BH；
  **CSCV 同时报告分区数 g ∈ {8, 12, 16} 的 PBO 曲线**（避免单粒度侥幸/冤案）。
- **完成判据**：全台账审计表 + 翻案/维持总表；F7.1 以此为统计依据。

---

### 预登记 #33（S6.1）截面参照系与抽样稳健性（仅报告）

- **网格（预登记）**：参照系 ∈ {批内 rank, 全市场 ECDF}；抽样种子 ∈ 5 个固定种子；maxn ∈ {500, 1000, 全取}；合计 2×5×3=30 组面板级差异统计（IC 均值/HAC t 的组间分布）。
- **假设**：H0=结论对这些实现选择不敏感；H1=敏感。仅报告不平判。

### 预登记 #34（S6.2）规则参数邻域平坦性（仅报告）

- **网格（预登记，自封）**：买入线 {65,70,75}，卖出线 {40,45,50}，CPPI 档位 ±5pp，trail {15%,20%,25%}，槽位 {8,10,12}；
  报告口径 = 基线位于邻域内的平坦度（邻域指标分布而非择优）；**本项明确禁止用于"再调参"**，输出只为脆弱性披露。

### 预登记 #35（S6.3）前瞻纸面验证（唯一真正外生 OOS）

- **协议**：起点 = 下一个自然决策月；每月固定时间运行当前生产模型，**记录且仅记录**当时 TopN 与 S；12 个月后（fwd6 成熟后）算纸面 IC/组合表现；
  期间禁止任何模型参数改动（改动即重置观察期）。
- **完成判据**：12 个月完整台账 + 按 §0.3（前瞻段 L 规则）统计；与历史 IC 分布对照（不裁决模型，仅入库证据）。

---

### 预登记 #36（F7.1–F7.3）终裁与勘误

- **F7.1 终版裁决报告**：每条结论附证据链（实验编号 → 产物 → 统计口径 → 适用边界）；明确区分"统计上成立 / 台账内相对最优 / 弱-中等强度"三档表述；
- **F7.2 README 与历史报告勘误**：按 R3/M1.2 勘误表逐条落实；原文不删，修订段并列；
- **F7.3 对外数字凡例**：每个对外引用数字 → {口径标签（FULL-PIT/SURV-ADJ/历史档案）、有效窗口、复现命令、已知局限}；
- **完成判据**：首页不再出现无口径标签的绝对收益/IC/胜率数字。

---

## 4. 执行日志（按时间追加，只增不改）

- **2026-09-01**：V5 重规范计划依据 `docs/审计报告_实验严谨性审查_2026-09-01.md`（F1–F5 / M1–M10）建立；
  全局协议（§0）与全部 **32 个实验项**完成预登记（预登记编号 **#10–#36**，R3.1–R3.4 共享 #23）。状态：全部 ⬜。
  可立即开工（离线可做）：M1.1 / M1.2 / M1.3 / M1.4 / D0.2 / D0.3 / D0.4 / B2.1 / B2.2 / B2.3 / R3.1–R3.5；
  数据受阻冻结：D0.1 / D0.5 → 连带 U4.* / A5.* 保持 ⬜ 直至解锁。

> 本计划只组织实验，不预设结果；一切采纳、否决、改写以按预登记门产出的真实数据为唯一依据。
