# V6 / T0 数据底座验收报告（滚动记录）

> 预登记：`docs/优化计划_V6_底层表示重构_2026-09.md` §3 T0、§5-R4
> 结论摘要：**T0 在当前沙箱内不可执行**，全部行情/名录数据源被网络策略阻断。
> 按 §5-R4 启动"用户本机抓取 + 沙箱并行做方法学"的分工，并开始计 M-T0 里程碑。

---

## 1. 环境重建（前置）

第三次沙箱重置：`cache/`（V5 记录曾有 15,444 只 nav）、`data/`、`output/*`（除 2 个 git 跟踪文件）、venv **全损**。
这与 `docs/V5_复跑事故与M11发现_2026-09-01.md` 记录的前两次同类。

| 项 | 结果 |
|---|---|
| venv | `/home/user/.venv_v6` 重建完成 |
| 钉版核对 | pandas 3.0.5 / numpy 2.4.6 / sklearn 1.9.0 / scipy 1.17.1 / statsmodels 0.15.0 —— 与 `requirements-v5-lock.txt` **逐项吻合** |
| 现存数据 | `cache/` 0 文件、`data/` 不存在、`output/` 仅 `model_zoo_report.md` + `scan_20260810.csv` + `v5/` |
| 测试基线 | `pytest tests/` → **47 passed, 1 skipped**（M1.1 闸门常绿） |

---

## 2. 数据源可达性探测（T0.1 步骤 1 要求"不得口头放弃，需留日志"）

探测时间 **2026-09-07T08:30:27Z**，产物 `output/v6/t0_source_probe.csv`。

| 主机 | HTTP | 判定 |
|---|---|---|
| api.fund.eastmoney.com | 000 | **BLOCKED** |
| push2his.eastmoney.com | 000 | **BLOCKED** |
| fundf10.eastmoney.com | 000 | **BLOCKED** |
| fund.eastmoney.com | 000 | **BLOCKED** |
| hq.sinajs.cn | 000 | **BLOCKED** |
| www.csindex.com.cn | 000 | **BLOCKED** |
| query1.finance.yahoo.com | 000 | **BLOCKED** |
| api.tushare.pro | 000 | **BLOCKED** |
| github.com | 200 | REACHABLE |
| files.pythonhosted.org | 200 | REACHABLE |

**结论**：沙箱出网白名单只含软件分发（PyPI/GitHub），**不含任何金融数据源**。
这不是接口反爬或限流，是网络层策略 —— 重试、换 UA、加 Referer 均无意义（已验证 `lsjz` 接口带 Referer 同样 000）。

**与 V5 的关系**：V5 的 D0.1/D0.3/D0.5 长期挂 🔶 正是同一根因。
本轮把它从"某次抓取失败"确认为**结构性环境约束**，因此不应再反复重试，而应直接走分工路径。

---

## 3. 处置：启动 §5-R4 分工（不是延期，是并行）

| 轨道 | 归属 | 内容 |
|---|---|---|
| **数据轨** | **用户本机执行** | `t0_fetch.py`（Tushare 全量 `fund_basic(status)` + `fund_nav`）、`d01_probe_em.py`（东财清盘基金覆盖抽样 50 只）、`d03_build_tr.py`（全收益基准）。产物回填 `cache/` 与 `data/pit_universe/` |
| **方法轨** | 沙箱内已推进 | T1.1 `dyn_rbsa.py` + 20 项不变量测试（全部合成数据，零外部依赖）。详见 `docs/V6_T1_动态基准报告.md` |

方法轨的合法性依据：§5-R4 明确"T1/T2 的**方法学开发**可在 SURV 池并行推进（代码+诊断，不出裁决数字）"。
本轮方法轨连 SURV 池都没用到（纯合成数据），更不触及裁决。

### 3.1 M-T0 里程碑开始计时

计划书 §5-R4 登记：**自开工起 4 周**（→ 2026-10-05）若 T0.1 仍不可达，
启动降级路径 **LOWER-BOUND-PIT**（已知清盘名单 + 最后净值截断构造下界池），
明确标注为**不可裁决口径**，仅用于灵敏度上下界。

**当前不启动降级**：距里程碑尚早，且降级池的信息价值远低于真实全池，
过早启用会污染后续 PBO 台账计数。

---

## 4. 用户侧待办（数据轨）

按优先级：

1. **T0.1 死亡基金净值** —— 本机跑 `t0_fetch.py`（需 Tushare 2000 积分账号；脚本已就绪）。
   验收：面板中死亡/转型基金占比 > 0（现状缓存层 **0 只死亡基金**，是整个 V6 的硬门）。
2. **T0.2 历史名录快照** —— 把 `data/pit_universe/` 从"推定型"（成立日 + 首披露推定，243 月末）
   升级为真实名录快照；无法升级的月份保留推定标签并逐月披露占比。
3. **T0.3 全收益基准** —— `d03_build_tr.py`（修复 V5 M1，现有 B2.1-R4 基准列仍为 `TR_PENDING`）。

产物回填后，沙箱内即可接续 T0.3 宇宙接入 → T0.4 面板重建 → T0.5 幸存者偏差计量 → 解锁 T1.2。

---

## 5. 状态表回写

| 编号 | 项 | 状态 | 结果 |
|---|---|---|---|
| T0.1 | 死亡基金净值获取 | 🔶 受阻 | 沙箱网络层阻断（取证 `output/v6/t0_source_probe.csv`）；转用户本机 |
| T0.2 | 历史名录快照升级 | 🔶 受阻 | 同上 |
| T0.3 | PiT 宇宙接入产线 | ⬜ | 阻塞于 T0.1/T0.2 |
| T0.4 | FULL-PIT 面板重建 | ⬜ | 阻塞 |
| T0.5 | 幸存者偏差计量 | ⬜ | 阻塞 |
