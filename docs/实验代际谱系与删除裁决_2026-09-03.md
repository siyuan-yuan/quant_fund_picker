# 实验代际谱系与删除裁决（取证版，2026-09-03）

> 性质：应需求"整理最初实验 vs 复盘重做实验，删除旧错误实验"而做的**取证与裁决文档**。
> 本文**不执行任何删除**；先给全谱系分类与删除风险，由你裁决后再逐个 `git rm`。
> 方法：依赖图（import/读产物）+ 复盘脚本引用面 + 审计病灶定位 + 报告引用面 四维交叉取证。

---

## 0. 结论先行（避免误删复盘链）

经逆向依赖取证，本仓库**不是"孤立的旧实验脚本 + 复盘产物"的二分结构**，而是：
复盘脚本（r31–r35/b2x/c_experiments/s6x/tests/m12b）**import 原始脚本的函数或读取其产物**。
因此"原始/含朴素t" ≠ "可删"。直接 `git rm` 会断复盘链、破坏"结论可复现到产物"的铁律。

唯一真正的**死代码**（审计已点名、零调用方）是 `pit_universe.py`，但它在多份文档中被引用为
"D0.5 未来接入的 PiT 底座"，是否删也需你定夺（见下）。

---

## 1. 分层总表（root *.py，65 个跟踪文件）

### A 层：引擎 / 产品 / 数据层 —— 不可删（webapp/holding_diag/scan_market 依赖）
| 文件 | 角色 | 删除影响 |
|---|---|---|
| config.py / engine.py / factors.py / risk.py / rbsa.py / provider.py / em_fetch.py / v8_guard.py / research_guard.py / scan_market.py / holding_diag.py / webapp.py / templates/ | 生产模型、Web、数据、护栏 | 断产品与全部下游 |
| sim_core.py / stats_hac.py | V5 统一模拟器/HAC 核心 | 复盘链唯一执行与统计口径 |
| pit_universe.py | **严格 PiT 底座（死代码，零调用）** | 见 §3 专议 |

### B 层：V5 复盘/重建脚本（权威）—— 不可删
build_navadj.py, p1_panel_build.py, p2_vol_build.py, p1_analysis.py, p2_analysis.py,
p4_analysis.py, r31_hac_redo.py, r32_hac_redo.py, r33_hac_redo.py, r34_hac_redo.py,
r35_e2e.py, r35_zoo_cache_shard.py, r35_zoo_redo.py, b21_baseline.py, b22_b23.py,
c_experiments.py, m12b_hac_remainder.py, m13_parity_strategy_bt.py,
m14_backfill_manifest.py, v5_manifest.py, s61_runner.py, s61_summarize.py, s61_verify.py,
s62_param_grid.py, s63_paper_registry.py, tests/test_research_invariants.py, d05_pit_universe_inferred.py

> 说明：p1_analysis/p2_analysis/p4_analysis 虽含朴素 t 口径，但被 r31–34/s61/tests **import 复用**
> （`_ic_row`/`FACTORS`/`load_panel`/`holm`…），删它们会让 HAC 复盘脚本无法逐位复现原始 IC 序列。

### C 层：复盘链的输入生成器（原始实验，产物被复盘读）—— 不建议删
| 文件 | 产物 | 被谁读 |
|---|---|---|
| factor_study.py | factor_variant_rows.csv | r34_hac_redo, m14 |
| p2_vol_build.py | output/p2/vol_panel.csv | r33_hac_redo |
| p4_alpha_decay.py | output/p4/hp4a_pit_monthly_ic.csv | m12b_hac_remainder |
| rbsa_ew_study.py | output/rbsa_ew_verdict.csv | m12b_hac_remainder |
| top100_history_pool.py | top100_history_pool.txt | backtest_local, r35_zoo_cache_shard(R3.5 种子池) |

### D 层：含朴素 t / 未复权等病灶、且未被复盘链 import 的原始实验 —— 本层才是删除候选
但需逐一对"是否仍被 V4 报告当结论引用 / 是否互为 import"核实：

| 文件 | 病灶 | 结论去向 | 删除风险 |
|---|---|---|---|
| wf_backtest.py | 朴素 t（F2）| 审计点名；结论已被 R3.4 HAC 取代 | 孤立（无 import）|
| wf_variants.py | 朴素 t | 同上一族 | import backtest |
| veto_study.py | 原始否决研究 | V4 报告引用其结论? | import backtest |
| tenure_study.py | 同上 | V4 报告引用? | import backtest |
| analyze_veto.py | 否决规则证据 | 读 output/veto_rows | 无 import |
| strategy_experiment.py | 未复权/T+0（F1/F5）| M1.3 已并轨到 sim_core | 无 import |
| p4_cap35_mech.py | 诊断脚本 | 终版报告 #8 | 无 import |
| backtest.py | build_pool"无幸存者偏差"错误标注（F3）| 审计点名 | 被 D 层 tenure/veto/wf_variants import |
| d01_probe_em.py / d03_build_tr.py / t0_fetch.py | 非实验，用户侧供数工具 | D0.1/D0.3 阻塞项 | 见 §3 |
| b22_b23.py 等已在 B 层 | — | — | — |

---

## 2. 报告代际（docs/，纪律 = 原文保留、勘误并列）

| 文档 | 代际 | 处置建议 |
|---|---|---|
| 优化计划_V4_2026-08.md | 最初主契约 | 保留（历史，V5 引用其预登记）|
| P1/P2/P3×3/P4×3 报告 | 最初实验报告 | **保留原文**（审计/勘误引用为证据）|
| 审计报告 | 复盘审查 | 保留（F1–F5/M1–M10 根因记录）|
| 勘误追加段_HAC复核 | 复盘勘误 | 保留（原文不删的并列载体）|
| 优化计划_V5 + 候选台账 + 执行台账 + 数据质量门 | V5 复盘 | 保留（唯一权威）|
| docs/artifacts/第三轮产物清单 | 产物指纹 | 保留（manifest 历史）|

> 依据仓库铁律：历史报告被审计与勘误当作"翻案前证据"，物理删除会破坏翻案链。**建议不删任何报告**。

---

## 3. 需你裁决的特殊项

1. **pit_universe.py**：审计实测"零调用死代码"。但文档把"接入产线(U4.1)"列为待办（D0.5）。
   → 选项：删(作废该框架) / 留(等 U4.1 接入真实名录快照)。
2. **d01_probe_em / d03_build_tr / t0_fetch / d05_pit_universe_inferred**：非"错误实验"，是**用户本机联网供数工具**（D0.1/D0.3/D0.5 阻塞项的钥匙）。删它们会让 P0 永远无法解锁。**建议必留**。
3. **backtest.py 簇（backtest/tenure/veto/wf_*) 互为 import**：若删必须整簇同删，否则留残；但整簇是 V4"否决规则/平滑化"结论的原始证据生成器，且被 V5 文档点名。**建议保留或整体归档，不逐个删**。

---

## 4. 我的建议（不预设，供裁）

- **强烈不建议物理删除任何 .py / 报告**：本仓库复盘链与历史证据深度耦合，"旧的"多是"复盘链的输入/代码依赖"。
- 若目标只是"让仓库不被旧文件干扰"，最安全 = **归档**：新增 `docs/history_archive/`，把 D 层脚本 + V4 报告移入并加头注"历史档案，V5 复盘证据"，**不 git rm**（历史仍可从 git 找回，工作树更清爽，审计链不破）。
- 若你坚持物理删除，请从下任选并确认，我再逐个 `git rm`。

---

*（文档待你裁决后回填实际处置。全程未删除任何文件。）*
